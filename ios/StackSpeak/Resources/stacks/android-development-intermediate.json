{
  "stack": "android-development-intermediate",
  "words": [
    {
      "id": "andr0010-0000-0000-0000-000000000001",
      "word": "Activities",
      "pronunciation": "ak-TIV-ih-teez",
      "partOfSpeech": "noun",
      "shortDefinition": "Entry point for user interaction representing a single focused screen in an app.",
      "simpleDefinition": "Core application component providing a window for UI rendering and user interaction — typically representing one full screen. Activities manage complex lifecycle transitions, serving as the primary container for layouts and handling user input events during the application's runtime.",
      "longDefinition": "Activities serve as the primary entry point for user interaction, managing the window where the UI is rendered. Each activity represents a distinct screen and follows a rigorous lifecycle to handle system-level events like rotation or low memory. Apps typically consist of multiple activities loosely coupled through Intents to facilitate navigation and seamless data sharing across the system.",
      "connector": "Think of activities like rooms in a house — each serves a specific purpose, and you navigate between them to perform different tasks.",
      "techContext": "The MainActivity is the entry point of our application, responsible for setting up the initial UI layout.",
      "exampleSentence": "The app crashed because the login activity was launched without being declared in the AndroidManifest.xml file.",
      "etymology": "from Latin activitas ('active state').",
      "codeExample": {
        "language": "kotlin",
        "code": "class MainActivity : AppCompatActivity() {\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        setContentView(R.layout.activity_main)\n    }\n}"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "fundamentals",
        "ui"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0020-0000-0000-0000-000000000002",
      "word": "Intent Filters",
      "pronunciation": "in-TENT FIL-turz",
      "partOfSpeech": "noun",
      "shortDefinition": "Manifest declarations that specify which implicit intents an Android component is willing to receive.",
      "simpleDefinition": "Declarations in the AndroidManifest.xml that advertise what kinds of requests a component — activity, service, or broadcast receiver — is capable of handling. When another app or the system fires an implicit intent, Android consults every registered intent filter to find the best-matching component to route the request to.",
      "longDefinition": "Intent filters bridge the loosely-coupled component model that makes Android apps composable. By declaring an intent filter with specific action, category, and data elements, a component signals its capabilities to the Android system. When an implicit intent is dispatched — for example, ACTION_SEND with a MIME type of 'image/*' — Android queries the PackageManager for all components whose filters match, then either starts the single match or presents a chooser dialog. Explicit intents (those that name a specific component class) bypass intent filter matching entirely. Intent filters are also how the system knows which activity to start as the app launcher entry point via ACTION_MAIN + CATEGORY_LAUNCHER.",
      "connector": "Think of intent filters like job listings — each component posts what kinds of work it can do, and Android acts as the recruiter who matches open requests to the right candidate.",
      "techContext": "Deep-link URLs, share-sheet integration, and app-to-app communication all rely on intent filters. The data element supports URI schemes (e.g., 'https') and host patterns, enabling apps to intercept links from the browser.",
      "exampleSentence": "The share button in our reading app uses an implicit intent, and our app's intent filter for ACTION_SEND with text/plain causes it to appear in the system share sheet.",
      "etymology": "from 'intent' (a declaration of purpose, from Latin intentus) + 'filter' (a device that passes matching items).",
      "codeExample": {
        "language": "xml",
        "code": "<activity android:name=\".ShareActivity\">\n    <intent-filter>\n        <action android:name=\"android.intent.action.SEND\" />\n        <category android:name=\"android.intent.category.DEFAULT\" />\n        <data android:mimeType=\"text/plain\" />\n    </intent-filter>\n</activity>"
      },
      "tags": [
        "android",
        "components",
        "navigation"
      ],
      "category": "components",
      "unlockLevel": 6
    },
    {
      "id": "andr0030-0000-0000-0000-000000000003",
      "word": "Jetpack Compose",
      "pronunciation": "JET-pak kum-POHZ",
      "partOfSpeech": "noun",
      "shortDefinition": "Modern declarative toolkit for building native Android user interfaces entirely in Kotlin.",
      "simpleDefinition": "Modern declarative UI toolkit for Android — allowing developers to build interfaces entirely in Kotlin without XML layouts. It simplifies UI development by automatically updating the screen whenever the underlying state changes, significantly reducing boilerplate code and increasing development velocity.",
      "longDefinition": "Jetpack Compose shifts UI development from an imperative model to a declarative one, where you describe what the UI should look like for a given state. It leverages Kotlin's powerful language features to provide a more intuitive and concise way to build complex layouts. By managing state updates automatically, it eliminates common bugs related to manual view synchronization.",
      "connector": "Think of Jetpack Compose like a smart recipe — you describe the dish and it automatically updates if you change an ingredient's quantity.",
      "techContext": "We refactored our settings screen using Jetpack Compose, which reduced our code by 50% compared to the old XML-based approach.",
      "exampleSentence": "We refactored our settings screen using Jetpack Compose, which reduced our UI code by nearly fifty percent.",
      "etymology": "from 'Jetpack' (Google's library suite) + 'Compose' (to create).",
      "codeExample": {
        "language": "kotlin",
        "code": "@Composable\nfun Greeting(name: String) {\n    Text(text = \"Hello $name!\", modifier = Modifier.padding(16.dp))\n}"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "ui",
        "jetpack",
        "kotlin"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0040-0000-0000-0000-000000000004",
      "word": "Gradle",
      "pronunciation": "GRAY-dul",
      "partOfSpeech": "noun",
      "shortDefinition": "Build automation system used for compiling, managing dependencies, and packaging Android apps.",
      "simpleDefinition": "Advanced build automation tool that compiles source code, manages complex library dependencies, and packages everything into installable Android binaries. It uses flexible DSL scripts to define the entire build pipeline and environment configurations, allowing for highly customized and efficient development workflows.",
      "longDefinition": "Gradle is the backbone of the Android build process, providing a powerful and flexible system for managing projects. It automates tasks like dependency resolution, resource processing, and APK signing. By using Groovy or Kotlin DSL, developers can script complex build logic to handle multiple build variants, flavors, and automated testing, ensuring a consistent and reproducible build environment.",
      "connector": "Think of Gradle like an assembly line foreman — it coordinates every step of the build, from gathering parts to final quality checks.",
      "techContext": "I updated the dependencies in the Gradle file to include the latest version of the Firebase SDK.",
      "exampleSentence": "I updated the dependencies in the Gradle file to include the latest version of the Firebase SDK for analytics.",
      "etymology": "Invented name, combining 'gradual' + 'build'.",
      "codeExample": {
        "language": "kotlin",
        "code": "dependencies {\n    implementation(\"androidx.core:core-ktx:1.10.1\")\n    testImplementation(\"junit:junit:4.13.2\")\n}"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "devops",
        "build-tools"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0050-0000-0000-0000-000000000005",
      "word": "Manifest",
      "pronunciation": "MAN-ih-fest",
      "partOfSpeech": "noun",
      "shortDefinition": "Required XML file describing essential application information to the Android OS.",
      "simpleDefinition": "Mandatory configuration file describing an app's essential components, required permissions, and hardware requirements to the Android OS. It serves as the primary declaration of what the app is and what it can access, ensuring the system can launch and manage it correctly.",
      "longDefinition": "The AndroidManifest.xml file is a crucial component that provides the Android system with necessary information about the application. It declares components like activities, services, and broadcast receivers, as well as requested permissions and hardware features. This metadata allows the OS to understand the app's structure and security requirements before any code is actually executed on the device.",
      "connector": "Think of the manifest like a passport — it identifies your app to Android and lists all its authorized capabilities and permissions.",
      "techContext": "I had to add the INTERNET permission to the manifest so our app could fetch the user's feed from our server.",
      "exampleSentence": "I had to add the INTERNET permission to the manifest so our app could fetch the user's feed from our server.",
      "etymology": "from Latin manifestus ('evident, caught in the act'), meaning a list of contents.",
      "codeExample": {
        "language": "xml",
        "code": "<manifest ...>\n    <uses-permission android:name=\"android.permission.INTERNET\"/>\n    <application ...>\n        <activity android:name=\".MainActivity\"/>\n    </application>\n</manifest>"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "fundamentals",
        "config"
      ],
      "category": "components"
    },
    {
      "id": "andr0060-0000-0000-0000-000000000006",
      "word": "APK",
      "pronunciation": "AY-PEE-KAY",
      "partOfSpeech": "acronym",
      "shortDefinition": "Standard file format used by Android OS for application distribution and installation.",
      "simpleDefinition": "The standard file format for Android application distribution — a compressed bundle containing compiled bytecode, resources, and the app manifest. It serves as the final installable package used by the Android OS to run software on devices, representing the complete application for end-user installation.",
      "longDefinition": "An Android Package (APK) is essentially a ZIP file that contains all the components necessary for an Android app to function. This includes the DEX files for the code, resources for the UI, and the manifest for configuration. While App Bundles are now used for publishing, the APK remains the format that is actually installed and executed on the device.",
      "connector": "Think of an APK like a boxed board game — it contains all the pieces and instructions needed to play, neatly packaged.",
      "techContext": "I sent the debug APK to the testing team so they could side-load it on their test devices.",
      "exampleSentence": "I sent the debug APK to the testing team so they could side-load it on their test devices for verification.",
      "etymology": "Acronym for Android Package.",
      "codeExample": {
        "language": "text",
        "code": "// APK is a file format; build configuration is managed in build.gradle."
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "fundamentals",
        "devops"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0070-0000-0000-0000-000000000007",
      "word": "Coroutines",
      "pronunciation": "koh-ROO-teenz",
      "partOfSpeech": "noun",
      "shortDefinition": "Concurrency design pattern simplifying asynchronous execution through non-blocking, sequential-style code.",
      "simpleDefinition": "Lightweight threads for managing long-running tasks asynchronously — enabling developers to write non-blocking code that reads like synchronous logic. They eliminate complex callback chains and handle main-thread switching automatically for smoother mobile user interfaces.",
      "longDefinition": "Coroutines provide a framework for managing background tasks without blocking the main thread or relying on deeply nested callbacks. By suspending execution instead of blocking a thread, they allow for high concurrency with minimal memory overhead. In Android, they integrate deeply with lifecycle-aware components to prevent memory leaks and ensure tasks cancel when no longer needed.",
      "connector": "Think of coroutines like waiters in a restaurant — they take an order then move to other tables while the kitchen prepares the meal.",
      "techContext": "We use Kotlin coroutines to perform network requests and database operations without freezing the application's UI.",
      "exampleSentence": "Using a coroutine with the Dispatchers.IO scope ensured that the file download didn't block the user's interaction with the app.",
      "etymology": "from 'co-' (together) + 'routines' (functions).",
      "codeExample": {
        "language": "kotlin",
        "code": "viewModelScope.launch {\n    val user = repository.getUser(id)\n    _userData.value = user\n}"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "kotlin",
        "concurrency"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0080-0000-0000-0000-000000000008",
      "word": "ViewModel",
      "pronunciation": "VYOO MAH-dul",
      "partOfSpeech": "noun",
      "shortDefinition": "Architecture component managing UI-related data in a lifecycle-conscious way for screens.",
      "simpleDefinition": "Jetpack component designed to store and manage UI data across configuration changes — ensuring state persistence when activities are recreated. It isolates business logic from the UI controller, preventing memory leaks and promoting cleaner, more testable code architectures during development.",
      "longDefinition": "ViewModels are designed to outlive the lifecycle of UI controllers like Activities and Fragments. This allows data to persist through configuration changes, such as screen rotations, without requiring expensive re-fetching or manual state saving. By acting as a bridge between the repository and the View, they ensure the UI remains responsive and synchronized with the latest data.",
      "connector": "Think of a ViewModel like a designated survivor — when the building is demolished and rebuilt, the survivor keeps all the information safe.",
      "techContext": "By moving the network call logic into the ViewModel, we ensured that the data isn't re-fetched when the user rotates their phone.",
      "exampleSentence": "The ViewModel communicates with the repository to fetch data and exposes it to the View via observable flows.",
      "etymology": "from 'View' + 'Model'.",
      "codeExample": {
        "language": "kotlin",
        "code": "class MyViewModel : ViewModel() {\n    private val _data = MutableStateFlow<String>(\"\")\n    val data = _data.asStateFlow()\n}"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "architecture",
        "jetpack"
      ],
      "category": "components"
    },
    {
      "id": "andr0090-0000-0000-0000-000000000009",
      "word": "LiveData",
      "pronunciation": "LYV DAY-tuh",
      "partOfSpeech": "noun",
      "shortDefinition": "Lifecycle-aware observable data holder class that only notifies active observers.",
      "simpleDefinition": "Observable data holder class specifically designed to be lifecycle-aware — only notifying active observers like 'Started' or 'Resumed' activities. This prevents common crashes and memory leaks by respecting the component's current state automatically without requiring manual observer management.",
      "longDefinition": "LiveData is an observable that respects the lifecycle of other app components, such as Activities, Fragments, or Services. This awareness ensures that LiveData only updates observers that are in an active lifecycle state. Consequently, it prevents issues like crashing due to stopped activities and eliminates the need for manual cleanup, making it a robust choice for UI data.",
      "connector": "Think of LiveData like a smart news ticker — it only scrolls when someone is actually in the room to read it.",
      "techContext": "Our ViewModel uses LiveData to provide the current weather information to the main activity.",
      "exampleSentence": "The activity automatically stopped receiving LiveData updates when it went into the background, saving system resources.",
      "etymology": "from 'live' + 'data'.",
      "codeExample": {
        "language": "kotlin",
        "code": "val name: MutableLiveData<String> by lazy {\n    MutableLiveData<String>()\n}"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "jetpack",
        "reactive"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0100-0000-0000-0000-000000000010",
      "word": "SQLite ORM",
      "pronunciation": "ESS-kyoo-lyt OH-AR-EM",
      "partOfSpeech": "noun",
      "shortDefinition": "A library that maps Kotlin objects to SQLite table rows, eliminating hand-written SQL for database operations.",
      "simpleDefinition": "An Object-Relational Mapper sitting on top of the device's built-in SQLite database — letting developers define tables as Kotlin data classes and perform queries through type-safe methods rather than raw SQL strings. On Android, Jetpack Room is the standard ORM, providing compile-time query validation and seamless Kotlin Flow and coroutine integration.",
      "longDefinition": "SQLite is the embedded relational database included in every Android device. Writing raw SQL against it by hand — using SQLiteOpenHelper — is verbose and error-prone, especially when schema versions change. An ORM (Object-Relational Mapper) abstracts those mechanics: you annotate Kotlin entities with @Entity to describe tables and @Dao interfaces to declare queries, and the library generates the boilerplate at compile time. Jetpack Room is the Google-recommended ORM for Android; it adds value over a bare ORM by checking SQL syntax at build time, supporting reactive query results as Flow<List<T>>, and managing database migrations with version-aware schema diffing. Room also integrates with Hilt for dependency injection and with Paging 3 for large dataset pagination.",
      "connector": "Think of an SQLite ORM like a blueprint translation service — you describe your data as Kotlin classes and it silently draws up the SQL contracts so you never have to write them by hand.",
      "techContext": "Room is the dominant Android ORM. Define @Entity data classes, write @Dao query interfaces, and declare a @Database class — Room generates the implementation. Use suspend functions in DAOs for coroutine support and Flow<T> return types to observe live database changes in the UI.",
      "exampleSentence": "Switching to Room meant the compiler caught a typo in our JOIN query at build time instead of throwing a runtime crash on the user's device.",
      "etymology": "from 'SQLite' (a compact embedded relational database by D. Richard Hipp) + 'ORM' (Object-Relational Mapping, a pattern from enterprise Java development circa 2000s).",
      "codeExample": {
        "language": "kotlin",
        "code": "@Entity(tableName = \"words\")\ndata class WordEntity(\n    @PrimaryKey val id: String,\n    val text: String,\n    val masteredAt: Long?\n)\n\n@Dao\ninterface WordDao {\n    @Query(\"SELECT * FROM words WHERE masteredAt IS NULL\")\n    fun observePending(): Flow<List<WordEntity>>\n\n    @Insert(onConflict = OnConflictStrategy.REPLACE)\n    suspend fun upsert(word: WordEntity)\n}"
      },
      "tags": [
        "android",
        "database",
        "jetpack"
      ],
      "category": "components",
      "unlockLevel": 6
    },
    {
      "id": "andr0110-0000-0000-0000-000000000011",
      "word": "Fragment",
      "pronunciation": "FRAG-munt",
      "partOfSpeech": "noun",
      "shortDefinition": "Grouping mechanism for child elements that avoids adding extra DOM nodes.",
      "simpleDefinition": "A syntax allowing developers to group multiple child elements without creating an extra wrapper node in the DOM. This prevents 'div soup' and keeps the document structure clean, which is essential for certain CSS layouts.",
      "longDefinition": "A specialized component that allows developers to group a list of children without adding extra nodes to the browser's Document Object Model. Since every React component must return a single root element, Fragments provide a way to return multiple elements without resorting to unnecessary wrapper divs, which can often break CSS layouts like Flexbox or Grid that rely on direct parent-child relationships.",
      "connector": "Think of Fragments like clear plastic wrap — it holds items together but is invisible once the meal (UI) is served.",
      "techContext": "Essential for keeping the DOM tree clean and avoiding 'div soup'.",
      "exampleSentence": "I used a Fragment to return two table cells from my component, as wrapping them in a div would be invalid HTML inside a row.",
      "etymology": "from Latin fragmentum ('a piece broken off').",
      "codeExample": {
        "language": "jsx",
        "code": "return (\n  <React.Fragment>\n    <ChildA />\n    <ChildB />\n  </React.Fragment>\n);\n// Or shorthand: <> ... </>"
      },
      "unlockLevel": 6,
      "tags": [
        "react",
        "dom",
        "layout"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0120-0000-0000-0000-000000000012",
      "word": "Dynamic Feature Modules",
      "pronunciation": "dy-NAM-ik FEE-chur MOD-yoolz",
      "partOfSpeech": "noun",
      "shortDefinition": "Gradle modules whose compiled code and resources are downloaded on demand after initial app install, reducing the size of the base APK.",
      "simpleDefinition": "A Play Core feature that splits an app into a lightweight base module and one or more optional modules whose code is only downloaded when a user actually needs them. This shrinks the install size users see on the Play Store and defers the cost of infrequently-used features — such as an AR camera or PDF export — until they are requested.",
      "longDefinition": "Dynamic Feature Modules (DFMs) are a component of the Android App Bundle format and the Play Core library. Rather than shipping all code in one monolithic APK, the app is split: a required base module contains critical code and resources, and each feature module contains the code for an independent section of the app. Modules can be installed-on-demand (downloaded when the user explicitly requests the feature), conditionally installed (based on device API level, language, or screen density at install time), or installed at first launch. The SplitInstallManager API handles requesting, monitoring, and cancelling module installs at runtime. Because Google Play reconstructs platform-optimized APKs from the bundle, users only ever download the native code, resources, and language strings their specific device requires — typically reducing download size by 20–40% compared to a universal APK.",
      "connector": "Think of dynamic feature modules like DLC in a video game — the base game installs immediately and you download additional content packs only when you choose to play them.",
      "techContext": "DFMs are defined as separate Gradle modules with 'apply plugin: com.android.dynamic-feature'. The base module lists them under 'dynamicFeatures' in its build.gradle. Use SplitInstallManager to trigger downloads at runtime and defer module initialization until installation is confirmed.",
      "exampleSentence": "We moved the heavy AR scanning feature into a dynamic feature module, cutting the initial download size by 35 MB and satisfying the Play Store's install-size recommendation.",
      "etymology": "from 'dynamic' (resolved at runtime, from Greek dynamikos) + 'feature module' (a bounded vertical slice of app functionality).",
      "codeExample": {
        "language": "kotlin",
        "code": "val splitInstallManager = SplitInstallManagerFactory.create(context)\n\nval request = SplitInstallRequest.newBuilder()\n    .addModule(\"ar_scanner\")\n    .build()\n\nsplitInstallManager.startInstall(request)\n    .addOnSuccessListener { sessionId ->\n        // Module downloaded; safe to launch AR activity\n    }\n    .addOnFailureListener { exception ->\n        // Handle install failure\n    }"
      },
      "tags": [
        "android",
        "app-bundle",
        "performance"
      ],
      "category": "components",
      "unlockLevel": 6
    },
    {
      "id": "andr0130-0000-0000-0000-000000000013",
      "word": "Retrofit",
      "pronunciation": "RET-roh-fit",
      "partOfSpeech": "noun",
      "shortDefinition": "Type-safe HTTP client for Android turning REST API endpoints into Kotlin interfaces.",
      "simpleDefinition": "Type-safe HTTP client library that turns REST API endpoints into simple Java or Kotlin interfaces. It automates the complex tasks of URL construction, request parameter mapping, and JSON parsing into structured data objects, making network communication seamless and much less error-prone.",
      "longDefinition": "Retrofit is the industry-standard library for network communication in Android development. By using annotations on interface methods, developers can define HTTP requests with minimal effort. It handles low-level details like connection management and header handling, while integrating with converters like Moshi or Gson to automatically translate JSON responses into usable data models, ensuring a clean and maintainable networking layer.",
      "connector": "Think of Retrofit like an automated switchboard — you just say who you want to talk to and it handles all the wiring.",
      "techContext": "We used Retrofit with Moshi to parse the JSON responses from our backend into our domain models.",
      "exampleSentence": "We used Retrofit with Moshi to parse the JSON responses from our backend into our domain models efficiently.",
      "etymology": "from 'retro-' (backwards) + 'fit', implying an addition of new parts to something old.",
      "codeExample": {
        "language": "kotlin",
        "code": "interface ApiService {\n    @GET(\"users/{id}\")\n    suspend fun getUser(@Path(\"id\") id: String): User\n}"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "networking",
        "library"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0140-0000-0000-0000-000000000014",
      "word": "Lifecycle",
      "pronunciation": "LYF-sy-kul",
      "partOfSpeech": "noun",
      "shortDefinition": "Stages a component traverses from creation to destruction in the DOM.",
      "simpleDefinition": "The predictable sequence of stages a component experiences — from being added to the DOM (mounting), through data updates, to being removed (unmounting). Developers use lifecycle hooks to manage side effects and resource cleanup.",
      "longDefinition": "The predictable series of phases every React component goes through: mounting (being added to the DOM), updating (reacting to changes in state or props), and unmounting (being removed from the DOM). Understanding these phases is crucial for managing side effects, such as starting network requests, setting up subscriptions, or cleaning up resources to prevent memory leaks, ensuring the application runs efficiently and reliably throughout its execution.",
      "connector": "Think of a lifecycle like a guest's visit — they arrive (mount), change plans (update), and eventually check out of the hotel.",
      "techContext": "In class components, these are explicit methods like componentDidMount. In function components, useEffect is used to handle these phases.",
      "exampleSentence": "We use the unmounting phase of the lifecycle to clean up active timers and network requests.",
      "etymology": "from 'life' + 'cycle' (a recurring series of events).",
      "codeExample": {
        "language": "js",
        "code": "useEffect(() => {\n  console.log('Mounted');\n  return () => console.log('Will Unmount');\n}, []);"
      },
      "unlockLevel": 6,
      "tags": [
        "react",
        "fundamentals",
        "side-effects"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0150-0000-0000-0000-000000000015",
      "word": "Kotlin Flow",
      "pronunciation": "KOT-lin FLOH",
      "partOfSpeech": "noun",
      "shortDefinition": "Cold asynchronous data stream that sequentially emits values over time.",
      "simpleDefinition": "Asynchronous data stream that sequentially emits values — built on top of coroutines for complex reactive programming. It supports sophisticated operators for filtering and transforming data, offering a more flexible and powerful alternative to standard LiveData for handling continuous data updates.",
      "longDefinition": "Built on top of coroutines, Flow is a cold stream that executes only when there is an active collector. It provides a rich set of operators to transform, filter, and combine data streams asynchronously. Unlike LiveData, Flow is not lifecycle-aware by default but can be integrated with lifecycle scopes to provide robust, reactive data handling.",
      "connector": "Think of Kotlin Flow like a conveyor belt — it delivers a steady stream of items one by one for you to process.",
      "techContext": "We used a StateFlow to represent the state of our UI, ensuring that the last emitted value is always available to new collectors.",
      "exampleSentence": "The repository returns a Flow of search results that we collect in the ViewModel using the collect function.",
      "etymology": "from 'Kotlin' + 'flow' (continuous movement).",
      "codeExample": {
        "language": "kotlin",
        "code": "fun getNumbers(): Flow<Int> = flow {\n    for (i in 1..3) {\n        delay(100)\n        emit(i)\n    }\n}"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "kotlin",
        "reactive",
        "async"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0160-0000-0000-0000-000000000016",
      "word": "Sealed Class",
      "pronunciation": "SEELD KLASS",
      "partOfSpeech": "noun",
      "shortDefinition": "A Kotlin class hierarchy whose subtypes are known and fixed at compile time.",
      "simpleDefinition": "A class you mark with the `sealed` modifier so the compiler knows every possible subclass — perfect for representing a closed set of states.",
      "longDefinition": "Sealed classes pair with exhaustive `when` expressions to give Android UI state a typesafe shape — Loading, Success, Error, etc. — without the boilerplate of enums or the runtime cost of arbitrary inheritance. The compiler refuses to build if you forget a branch.",
      "techContext": "Idiomatic Android architecture (MVI, MVVM with state) leans on sealed classes for UI state and one-shot events. Pairs naturally with StateFlow + Jetpack Compose.",
      "exampleSentence": "Model the screen state as a sealed class so the compiler forces every consumer to handle Loading, Success, and Error.",
      "etymology": "From `sealed` — the inheritance hierarchy is closed (sealed) at the file or module boundary.",
      "connector": "Like a finite state machine made type-safe — every transition spelled out, no fall-through case.",
      "codeExample": {
        "language": "kotlin",
        "code": "sealed class UiState {\n    object Loading : UiState()\n    data class Success(val data: List<Item>) : UiState()\n    data class Error(val msg: String) : UiState()\n}\n\nwhen (state) {\n    is UiState.Loading -> showSpinner()\n    is UiState.Success -> render(state.data)\n    is UiState.Error -> showError(state.msg)\n}"
      },
      "unlockLevel": 6,
      "tags": [
        "kotlin",
        "android",
        "state-management"
      ],
      "category": "patterns"
    },
    {
      "id": "andr0170-0000-0000-0000-000000000017",
      "word": "Broadcast Receiver",
      "pronunciation": "BRAWD-kast ree-SEE-vur",
      "partOfSpeech": "noun",
      "shortDefinition": "Component responding to system-wide broadcast announcements or application-level events.",
      "simpleDefinition": "Component enabling applications to receive and respond to system-wide broadcast messages — such as low battery alerts or connectivity changes. It acts as an event listener for triggers occurring outside the application itself, allowing the app to react dynamically to various system conditions.",
      "longDefinition": "Broadcast receivers allow apps to listen for system or application-wide events, such as boot completion or network changes. They serve as an entry point for the app to react to external triggers even when it is not currently running. By registering specific intent filters, developers can ensure their apps respond only to relevant events, maintaining efficiency and performance.",
      "connector": "Think of a broadcast receiver like a pager — it sits silent until a specific code is broadcast, then it wakes up to take action.",
      "techContext": "We registered a broadcast receiver to listen for the 'BOOT_COMPLETED' event so our app can reschedule its background alarms.",
      "exampleSentence": "The broadcast receiver triggered a notification as soon as the device connected to a power outlet.",
      "etymology": "from 'broadcast' (to scatter widely) + 'receiver'.",
      "codeExample": {
        "language": "kotlin",
        "code": "class MyReceiver : BroadcastReceiver() {\n    override fun onReceive(context: Context, intent: Intent) {\n        // Handle the received event here\n    }\n}"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "fundamentals",
        "messaging"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0180-0000-0000-0000-000000000018",
      "word": "WorkManager",
      "pronunciation": "WURK MAN-ij-ur",
      "partOfSpeech": "noun",
      "shortDefinition": "Library for managing persistent background tasks that must run even if the app closes.",
      "simpleDefinition": "Jetpack library for managing persistent background tasks that must run even if the app closes or the device restarts. It automatically selects the best system-level execution method based on API levels and current constraints, ensuring that critical operations are completed reliably.",
      "longDefinition": "WorkManager is the recommended solution for background work that is deferrable but requires guaranteed execution. It handles the complexity of different Android versions by choosing the appropriate API, such as JobScheduler or AlarmManager, to perform the task. By supporting constraints like network availability or charging status, it ensures that background work is performed efficiently without draining the battery.",
      "connector": "Think of WorkManager like a personal assistant with a long memory — you give them a task and they'll eventually finish it.",
      "techContext": "We used WorkManager to schedule a daily backup of the local database to the cloud.",
      "exampleSentence": "We used WorkManager to schedule a daily backup of the local database to the cloud storage service.",
      "etymology": "from 'work' + 'manager'.",
      "codeExample": {
        "language": "kotlin",
        "code": "val uploadWorkRequest = OneTimeWorkRequestBuilder<UploadWorker>().build()\nWorkManager.getInstance(context).enqueue(uploadWorkRequest)"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "jetpack",
        "background-tasks"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0190-0000-0000-0000-000000000019",
      "word": "NavGraph",
      "pronunciation": "NAV-graf",
      "partOfSpeech": "noun",
      "shortDefinition": "A Jetpack Navigation resource that maps all destinations and the transitions between them in a single visual graph.",
      "simpleDefinition": "A declarative XML or Kotlin DSL resource that describes every screen (destination) in an app and the possible paths (actions) between them. The Jetpack Navigation component reads this graph at runtime to handle back-stack management, deep-link routing, and safe argument passing automatically.",
      "longDefinition": "The Navigation Graph is the central artifact of the Jetpack Navigation component. It defines: destinations (fragments, activities, or composables that the user can navigate to), actions (the navigable connections between destinations, optionally carrying typed arguments via SafeArgs), and nested graphs (sub-graphs that group a logical flow such as onboarding). At runtime a NavController reads the graph and handles FragmentTransactions, back-stack manipulation, animated transitions, and deep-link resolution without any boilerplate in calling code. With Jetpack Compose, NavHost composable renders the graph and NavController.navigate() replaces manual back-stack management. Because all destinations are declared centrally, the graph also serves as living architecture documentation for the app's navigation structure.",
      "connector": "Think of a NavGraph like an airport terminal map — every gate (destination) is marked, every connecting corridor (action) is drawn, and the control tower (NavController) ensures passengers never board the wrong plane.",
      "techContext": "Define the graph in 'res/navigation/nav_graph.xml' or in Kotlin DSL with 'NavHost'. Use 'NavController.navigate(R.id.action_foo_to_bar)' to trigger transitions. Pass arguments using the generated SafeArgs Gradle plugin to eliminate string-keyed bundle errors at compile time.",
      "exampleSentence": "By moving all screen transitions into the NavGraph, we eliminated a dozen manual FragmentTransactions and got deep-link support to the detail screen for free.",
      "etymology": "Portmanteau of 'Navigation' + 'Graph' (a mathematical structure of nodes and edges), introduced as part of Jetpack Navigation in 2018.",
      "codeExample": {
        "language": "xml",
        "code": "<!-- res/navigation/nav_graph.xml -->\n<navigation xmlns:android=\"http://schemas.android.com/apk/res/android\"\n    app:startDestination=\"@id/homeFragment\">\n\n    <fragment android:id=\"@+id/homeFragment\"\n        android:name=\".HomeFragment\">\n        <action android:id=\"@+id/action_home_to_detail\"\n            app:destination=\"@id/detailFragment\" />\n    </fragment>\n\n    <fragment android:id=\"@+id/detailFragment\"\n        android:name=\".DetailFragment\">\n        <argument android:name=\"wordId\"\n            app:argType=\"string\" />\n    </fragment>\n\n</navigation>"
      },
      "tags": [
        "android",
        "navigation",
        "jetpack"
      ],
      "category": "components",
      "unlockLevel": 6
    },
    {
      "id": "andr0200-0000-0000-0000-000000000020",
      "word": "Data Binding",
      "pronunciation": "DAY-tuh BYN-ding",
      "partOfSpeech": "noun",
      "shortDefinition": "Technique binding UI components in layouts directly to application data sources automatically.",
      "simpleDefinition": "Technique allowing UI components in layouts to directly reference data sources — eliminating the need for boilerplate code like 'findViewById.' It automates UI updates by synchronizing the layout directly with changes in the underlying data objects, leading to cleaner code and fewer manual errors.",
      "longDefinition": "Data Binding allows developers to bind UI components in layouts to data sources using a declarative format rather than programmatically. This reduces the need for manual view updates and boilerplate code in the UI controller. By establishing a direct link between the data and the layout, it ensures the UI is always up-to-date with the latest information.",
      "connector": "Think of data binding like a puppet with strings — when you move the control, the puppet moves perfectly in sync.",
      "techContext": "With data binding, we linked the User object directly to the XML layout, so the name field updates automatically when the object changes.",
      "exampleSentence": "Data binding helped us eliminate hundreds of lines of code that were just copying data from the ViewModel to views.",
      "etymology": "from 'data' + 'binding' (fastening together).",
      "codeExample": {
        "language": "xml",
        "code": "<layout>\n    <data>\n        <variable name=\"user\" type=\"com.example.User\"/>\n    </data>\n    <TextView android:text=\"@{user.name}\" ... />\n</layout>"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "ui",
        "jetpack"
      ],
      "category": "processes"
    },
    {
      "id": "andr0210-0000-0000-0000-000000000021",
      "word": "Hilt",
      "pronunciation": "HILT",
      "partOfSpeech": "noun",
      "shortDefinition": "Dependency injection library for Android built on Dagger to simplify object provisioning.",
      "simpleDefinition": "Dependency injection library built on Dagger that simplifies object provisioning across an Android application. It provides standard containers for every Android class and automatically manages their lifecycles, reducing boilerplate and improving code testability by automating the injection of necessary dependencies.",
      "longDefinition": "Hilt provides a standard way to incorporate Dagger dependency injection into an Android application by handling the complex boilerplate usually associated with manual injection. It integrates seamlessly with Jetpack components and automatically manages the lifecycle of injected objects. This leads to a more modular, maintainable, and testable codebase by ensuring that each class receives its required dependencies.",
      "connector": "Think of Hilt like a tool belt — it ensures every worker has exactly the tools they need without them having to build them.",
      "techContext": "We used Hilt to inject our networking and database dependencies into our ViewModels, making the code much easier to test.",
      "exampleSentence": "We used Hilt to inject our networking and database dependencies into our ViewModels, making the code much easier to test.",
      "etymology": "from 'hilt' (the handle of a tool), implying something that makes Dagger easier to hold.",
      "codeExample": {
        "language": "kotlin",
        "code": "@HiltAndroidApp\nclass MyApplication : Application()\n\n@Inject\nlateinit var apiService: ApiService"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "di",
        "jetpack",
        "architecture"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0220-0000-0000-0000-000000000022",
      "word": "ProGuard",
      "pronunciation": "PROH-gard",
      "partOfSpeech": "noun",
      "shortDefinition": "Tool that shrinks, optimizes, and obfuscates code to reduce size and improve security.",
      "simpleDefinition": "Code shrinking and obfuscation tool that removes unused classes and renames members with meaningless identifiers — reducing binary size and protecting against reverse engineering. It essentially cleans up and secures the final production build by making the source code much harder to understand.",
      "longDefinition": "ProGuard (and its successor R8) is essential for optimizing Android applications for production. It performs four main tasks: shrinking, which removes unused code; optimization, which improves performance at the bytecode level; obfuscation, which renames classes and methods to thwart reverse engineering; and pre-verification. This results in a smaller, faster, and more secure application that is harder for malicious actors to decompile.",
      "connector": "Think of ProGuard like a professional shredder — it destroys the unnecessary documents and scrambles the important ones so they can't be read.",
      "techContext": "We enabled ProGuard in our production build to prevent competitors from easily decompiling our proprietary algorithms.",
      "exampleSentence": "We enabled ProGuard in our production build to prevent competitors from easily decompiling our proprietary sync algorithms.",
      "etymology": "from 'pro' + 'guard' (to protect).",
      "codeExample": {
        "language": "kotlin",
        "code": "buildTypes {\n    release {\n        isMinifyEnabled = true\n        proguardFiles(getDefaultProguardFile(\"proguard-android.txt\"), \"proguard-rules.pro\")\n    }\n}"
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "security",
        "build-tools"
      ],
      "category": "components"
    },
    {
      "id": "andr0230-0000-0000-0000-000000000023",
      "word": "Content Resolver",
      "pronunciation": "KON-tent ree-ZOL-vur",
      "partOfSpeech": "noun",
      "shortDefinition": "The Android system API that mediates cross-process data access between apps and Content Providers using URI-based queries.",
      "simpleDefinition": "A system-level client object that allows an app to read, insert, update, or delete data exposed by a Content Provider — including built-in providers for contacts, media, and calendar — using a standard URI-based CRUD interface. It acts as the intermediary that routes each request to the correct provider process, enforcing the permissions declared in the provider's manifest.",
      "longDefinition": "Content Resolver is the client-side half of Android's Content Provider architecture. An app retrieves a ContentResolver instance via Context.getContentResolver() and issues operations — query(), insert(), update(), delete(), and openInputStream() — against content:// URIs. The system looks up the authority portion of the URI to find the matching ContentProvider (which may live in another process), performs a Binder IPC call, and returns the result as a Cursor or Uri. This design lets apps share structured data without exposing raw database files. Common built-in providers accessed via ContentResolver include MediaStore (photos, videos, audio), ContactsContract (contacts and call logs), and CalendarContract (events). For observing changes, ContentObserver can be registered against a URI so the UI reacts when the underlying data changes.",
      "connector": "Think of ContentResolver like a library card system — you ask the librarian (resolver) for a book by ISBN (URI), it routes your request to the correct department (provider), checks your borrowing rights (permissions), and hands you the book (Cursor).",
      "techContext": "Use ContentResolver with MediaStore to access photos on Android 10+. Runtime permissions (READ_MEDIA_IMAGES on API 33+) must be granted first. Always close Cursor objects in a finally block or use the use { } extension to avoid resource leaks.",
      "exampleSentence": "We used ContentResolver to query MediaStore for images taken in the last 24 hours so the app could suggest them for the daily highlight card.",
      "etymology": "from 'content' (structured data exposed by a provider) + 'resolver' (an object that resolves a URI to the correct provider, analogous to DNS resolving a hostname to an IP address).",
      "codeExample": {
        "language": "kotlin",
        "code": "val projection = arrayOf(\n    MediaStore.Images.Media._ID,\n    MediaStore.Images.Media.DISPLAY_NAME,\n    MediaStore.Images.Media.DATE_TAKEN\n)\nval selection = \"${MediaStore.Images.Media.DATE_TAKEN} >= ?\"\nval args = arrayOf(System.currentTimeMillis() - 86_400_000L)\n\ncontentResolver.query(\n    MediaStore.Images.Media.EXTERNAL_CONTENT_URI,\n    projection, selection, args.map { it.toString() }.toTypedArray(),\n    \"${MediaStore.Images.Media.DATE_TAKEN} DESC\"\n)?.use { cursor ->\n    val idCol = cursor.getColumnIndexOrThrow(MediaStore.Images.Media._ID)\n    while (cursor.moveToNext()) {\n        val uri = ContentUris.withAppendedId(\n            MediaStore.Images.Media.EXTERNAL_CONTENT_URI,\n            cursor.getLong(idCol)\n        )\n        // use uri\n    }\n}"
      },
      "tags": [
        "android",
        "data",
        "ipc"
      ],
      "category": "components",
      "unlockLevel": 6
    },
    {
      "id": "andr0240-0000-0000-0000-000000000024",
      "word": "ART",
      "pronunciation": "ART",
      "partOfSpeech": "acronym",
      "shortDefinition": "Managed runtime environment executing Android applications using Ahead-of-Time compilation.",
      "simpleDefinition": "The managed runtime environment that executes Android applications — using Ahead-of-Time (AOT) compilation to turn bytecode into machine instructions upon installation. This improves overall system performance and battery efficiency compared to older interpretation methods by reducing the CPU load during app execution.",
      "longDefinition": "ART replaced the Dalvik VM as the default runtime for Android. By compiling bytecode into native machine code at installation time, ART reduces the need for Just-In-Time compilation during runtime. This leads to faster app startup times and improved execution speed. Additionally, ART features enhanced garbage collection and better debugging support, contributing to a more stable system.",
      "connector": "Think of ART like a translator who studies the whole book before you arrive — so they can speak to you instantly.",
      "techContext": "Because of ART's AOT compilation, Android apps launch faster and use less CPU during execution compared to the old Dalvik runtime.",
      "exampleSentence": "Because of ART's AOT compilation, Android apps launch faster and use less CPU compared to the old Dalvik runtime.",
      "etymology": "Acronym for Android Runtime.",
      "codeExample": {
        "language": "text",
        "code": "// ART is a runtime environment; no direct code example is applicable for standard app development."
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "internals",
        "performance"
      ],
      "category": "concepts"
    },
    {
      "id": "andr0250-0000-0000-0000-000000000025",
      "word": "Kotlin Multiplatform",
      "pronunciation": "KOT-lin MUL-tee-plat-form",
      "partOfSpeech": "noun",
      "shortDefinition": "Technology for sharing core business logic across Android, iOS, Web, and Desktop platforms.",
      "simpleDefinition": "Technology enabling developers to share a single codebase for core business logic, networking, and data across multiple platforms — including Android, iOS, and Web. It prioritizes code reuse while allowing for native UI development, ensuring that each platform maintains its unique look and feel.",
      "longDefinition": "Kotlin Multiplatform (KMP) allows for the sharing of non-UI code between different platforms, reducing duplication and maintenance effort. Unlike other cross-platform frameworks, KMP does not impose a specific UI layer, allowing developers to use native tools like SwiftUI for iOS and Jetpack Compose for Android. This approach combines the efficiency of code sharing with the performance of native applications.",
      "connector": "Think of Kotlin Multiplatform like a universal language for laws — the rules are the same everywhere, even if the buildings look different.",
      "techContext": "By using KMP, we were able to write our data sync logic once and use it in both our Android and iOS applications.",
      "exampleSentence": "By using KMP, we were able to write our data sync logic once and use it in both apps.",
      "etymology": "from 'Kotlin' + 'multiplatform' (many platforms).",
      "codeExample": {
        "language": "kotlin",
        "code": "// Shared code in commonMain:\nexpect fun getPlatformName(): String\n\n// Platform code in androidMain:\nactual fun getPlatformName() = \"Android\""
      },
      "unlockLevel": 6,
      "tags": [
        "android",
        "kotlin",
        "cross-platform"
      ],
      "category": "components"
    }
  ]
}
