{
  "chapterId": "rfc-ch04-moving-features",
  "shardIndex": 1,
  "cards": [
    {
      "id": "rfc-ch04-c001",
      "order": 1,
      "title": "Behavior Follows Data",
      "teaser": "The guiding principle of this chapter is that a method should live in the class whose data it mostly uses — everything else in this chapter is an application of that rule.",
      "explanation": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "When you look at a method and notice it spends most of its time reaching into another object's fields, that method is in the wrong place. It belongs next to the data it needs."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "This is the \"behavior follows data\" principle, and it shapes every refactoring in this chapter:"
            }
          ]
        },
        {
          "type": "list",
          "style": "bulleted",
          "items": [
            [
              {
                "text": "Move Method",
                "marks": [
                  "bold"
                ]
              },
              {
                "text": " applies it directly — you physically relocate a method to the class whose state it manipulates."
              }
            ],
            [
              {
                "text": "Move Field",
                "marks": [
                  "bold"
                ]
              },
              {
                "text": " applies it to data — when a field is more tightly coupled to a different class, move the field and update the accessors."
              }
            ],
            [
              {
                "text": "Extract Class",
                "marks": [
                  "bold"
                ]
              },
              {
                "text": " applies it to an overloaded class — when a class has grown two distinct data clusters, you split it so each cluster has its own class with its own behavior."
              }
            ],
            [
              {
                "text": "Hide Delegate",
                "marks": [
                  "bold"
                ]
              },
              {
                "text": " applies it defensively — when clients reach through one object to manipulate another, you let the owning class handle the delegation itself."
              }
            ]
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "The opposing failure mode is the anemic domain model: classes that carry data but no behavior, with all the logic pushed into service objects or controllers that do nothing but manipulate foreign state. That arrangement is feature envy made structural — the service class exists solely to reach into the data class and do what the data class should be doing itself."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "You don't have to resolve every case perfectly. The goal is directional: each move should reduce how often a method has to reach outside its own class to get work done."
            }
          ]
        },
        {
          "type": "callout",
          "variant": "info",
          "runs": [
            {
              "text": "Feature envy is the symptom; a misplaced method is the diagnosis; Move Method is the prescription. The other refactorings in this chapter handle the structural cases where a single move is not enough."
            }
          ]
        }
      ],
      "feynman": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Behavior follows data the same way a kitchen belongs next to a pantry — it makes no sense to cook in one room when all the ingredients are stored in another."
            }
          ]
        }
      ]
    },
    {
      "id": "rfc-ch04-c002",
      "order": 2,
      "title": "Move Method",
      "teaser": "When a method uses more data from a different class than from its own, move the method to the class it most depends on.",
      "explanation": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "The signal for Move Method is feature envy: you open a method and see it making three or four calls into another object's fields or methods while barely touching "
            },
            {
              "text": "this",
              "marks": [
                "code"
              ]
            },
            {
              "text": ". The method has already voted with its feet — it belongs in the other class."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Mechanical recipe:",
              "marks": [
                "bold"
              ]
            }
          ]
        },
        {
          "type": "list",
          "style": "numbered",
          "items": [
            [
              {
                "text": "Identify the target class — usually the one the method references most. If it's ambiguous, ask which class would make the method's name make the most sense."
              }
            ],
            [
              {
                "text": "Define the equivalent method on the target class. Copy the body over; replace references to the source object with parameters or direct field access as appropriate."
              }
            ],
            [
              {
                "text": "Decide how the source class will call the new method: delegate to it, accept a reference, or remove the call entirely if it was only needed in certain paths."
              }
            ],
            [
              {
                "text": "Replace the original method body with a delegation call to the target, then verify all tests pass."
              }
            ],
            [
              {
                "text": "Once callers are updated, delete the original method."
              }
            ]
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Be honest about when to stop: if the source class still uses the method's logic heavily — even if the method also reaches into the target — the move just relocates the envy without fixing it. That's a sign to Extract Class instead."
            }
          ]
        },
        {
          "type": "code",
          "language": "ts",
          "code": "// Before — ShippingCalculator reaches deep into Order\nclass ShippingCalculator {\n  computeCost(order: Order): number {\n    const weight = order.items.reduce((sum, item) => sum + item.weight, 0);\n    const zone = order.destination.shippingZone;\n    return weight * ZONE_RATES[zone];\n  }\n}\n\n// After — the method lives with the data it uses\nclass Order {\n  computeShippingCost(): number {\n    const weight = this.items.reduce((sum, item) => sum + item.weight, 0);\n    const zone = this.destination.shippingZone;\n    return weight * ZONE_RATES[zone];\n  }\n}"
        },
        {
          "type": "callout",
          "variant": "warning",
          "runs": [
            {
              "text": "Move Method is wrong when the original class still uses the method's data as heavily as the target does. Moving it just relocates the feature envy — recognize that as a signal to Extract Class instead."
            }
          ]
        }
      ],
      "feynman": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Moving a method to the class whose data it uses is like reassigning a project manager to the team they spend all day talking to — the work stays the same, but the communication overhead drops to zero."
            }
          ]
        }
      ]
    },
    {
      "id": "rfc-ch04-c003",
      "order": 3,
      "title": "Move Field",
      "teaser": "When a field is queried or set more often by a different class than by its own, move the field to where it actually lives.",
      "explanation": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "A field that is constantly accessed from another class is a structural smell. It means the owning class is acting as a data bucket for state that really belongs to a neighbor. Move Field fixes the address."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Mechanical recipe:",
              "marks": [
                "bold"
              ]
            }
          ]
        },
        {
          "type": "list",
          "style": "numbered",
          "items": [
            [
              {
                "text": "Find all read and write sites for the field across the codebase."
              }
            ],
            [
              {
                "text": "Create the field on the target class, with a matching accessor."
              }
            ],
            [
              {
                "text": "Update the source class to delegate reads and writes through the target's accessor rather than holding the value itself."
              }
            ],
            [
              {
                "text": "Verify all tests pass with the delegating version."
              }
            ],
            [
              {
                "text": "Remove the source field and any now-redundant delegating accessors once all callers have been updated."
              }
            ]
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "The tricky case is shared state: if two classes both have legitimate ownership of the field (reading and writing it roughly equally), that's a sign to Extract Class to create an explicit shared concept rather than picking an arbitrary winner."
            }
          ]
        },
        {
          "type": "code",
          "language": "ts",
          "code": "// Before — Order holds discountRate but Pricing always reads it\nclass Order {\n  discountRate: number;\n  // ...\n}\nclass Pricing {\n  apply(order: Order): number {\n    return order.totalBeforeDiscount * (1 - order.discountRate);\n  }\n}\n\n// After — discountRate moves to Pricing where it is used\nclass Pricing {\n  discountRate: number;\n  apply(order: Order): number {\n    return order.totalBeforeDiscount * (1 - this.discountRate);\n  }\n}"
        },
        {
          "type": "callout",
          "variant": "tip",
          "runs": [
            {
              "text": "If moving the field makes you realize the source class no longer has much state left, consider whether the source class itself should be inlined into the target — that's the Inline Class refactoring."
            }
          ]
        }
      ],
      "feynman": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Moving a field to the class that uses it is like moving the spare key to the house of the person who always needs to borrow it — the key belongs where it gets used, not where it was originally stored."
            }
          ]
        }
      ]
    },
    {
      "id": "rfc-ch04-c004",
      "order": 4,
      "title": "Extract Class",
      "teaser": "When a single class is doing two distinct jobs — managing its own identity and managing a separate cluster of behavior — split it into two classes, each with a clear purpose.",
      "explanation": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "The signal is a class with too many fields and methods where you can draw a mental line between two separate responsibilities. The two groups of fields and methods have little reason to talk to each other; they just happen to live in the same file."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Mechanical recipe:",
              "marks": [
                "bold"
              ]
            }
          ]
        },
        {
          "type": "list",
          "style": "numbered",
          "items": [
            [
              {
                "text": "Name the new class — the right name often makes it obvious you should have split earlier."
              }
            ],
            [
              {
                "text": "Create the new class and move the fields that belong to it. Update the original class to hold a reference to the new class."
              }
            ],
            [
              {
                "text": "Move the methods that operate on those fields, one at a time. After each move, run tests."
              }
            ],
            [
              {
                "text": "Review the interface between the two classes. Decide whether the original class should expose the new class directly or only delegate to it."
              }
            ],
            [
              {
                "text": "Tighten access modifiers — if the new class is an implementation detail, keep it package-private or unexposed."
              }
            ]
          ]
        },
        {
          "type": "code",
          "language": "ts",
          "code": "// Before — Order mixes order identity with physical shipping data\nclass Order {\n  id: string;\n  customerEmail: string;\n  shippingStreet: string;\n  shippingCity: string;\n  shippingPostalCode: string;\n  shippingCountry: string;\n  formattedAddress(): string { ... }\n  validateAddress(): boolean { ... }\n}\n\n// After — address concerns are extracted\nclass ShippingAddress {\n  street: string;\n  city: string;\n  postalCode: string;\n  country: string;\n  formatted(): string { ... }\n  isValid(): boolean { ... }\n}\n\nclass Order {\n  id: string;\n  customerEmail: string;\n  shippingAddress: ShippingAddress;\n}"
        },
        {
          "type": "callout",
          "variant": "info",
          "runs": [
            {
              "text": "Naming the new class before you start is the most important step. If you cannot name it clearly, you may not have found the right split yet — the right cut usually becomes obvious once you identify the two responsibilities."
            }
          ]
        }
      ],
      "feynman": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Extracting a class from an overloaded one is like splitting a general store into a grocery and a hardware shop — the same inventory existed before, but now each shop knows exactly what it sells."
            }
          ]
        }
      ]
    },
    {
      "id": "rfc-ch04-c005",
      "order": 5,
      "title": "Inline Class",
      "teaser": "When a class no longer carries enough responsibility to justify its existence, fold it back into the class that uses it.",
      "explanation": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Inline Class is Extract Class in reverse. A class that started as a useful abstraction can become a hollow wrapper after features migrate away — it holds one or two fields and delegates everything. At that point it adds navigational overhead without adding clarity."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Mechanical recipe:",
              "marks": [
                "bold"
              ]
            }
          ]
        },
        {
          "type": "list",
          "style": "numbered",
          "items": [
            [
              {
                "text": "Identify all callers of the class to be inlined. If there are many diverse callers, inlining may increase coupling — consider whether the class is actually serving a boundary that should be kept."
              }
            ],
            [
              {
                "text": "Move all the fields and methods of the thin class into the absorbing class, one at a time."
              }
            ],
            [
              {
                "text": "Update callers to use the absorbing class directly rather than going through the indirection."
              }
            ],
            [
              {
                "text": "Run tests after each move."
              }
            ],
            [
              {
                "text": "Delete the now-empty class."
              }
            ]
          ]
        },
        {
          "type": "code",
          "language": "ts",
          "code": "// Before — PhoneFormatter exists just to hold one method\nclass PhoneFormatter {\n  format(number: string): string {\n    return number.replace(/(\\d{3})(\\d{3})(\\d{4})/, '($1) $2-$3');\n  }\n}\n\nclass Customer {\n  private formatter = new PhoneFormatter();\n  displayPhone(): string {\n    return this.formatter.format(this.rawPhone);\n  }\n}\n\n// After — format logic lives directly in Customer\nclass Customer {\n  displayPhone(): string {\n    return this.rawPhone.replace(/(\\d{3})(\\d{3})(\\d{4})/, '($1) $2-$3');\n  }\n}"
        },
        {
          "type": "callout",
          "variant": "warning",
          "runs": [
            {
              "text": "Do not inline a class just because it is small. Size is not the criterion — ask whether the class represents a concept worth naming. A small class with a strong name and a clear contract is fine; a small class that is just a renamed method is not."
            }
          ]
        }
      ],
      "feynman": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Inlining a class that no longer carries its weight is like removing an intermediary from a supply chain once they stop adding value — the product still moves, but there is one fewer hand in between."
            }
          ]
        }
      ]
    },
    {
      "id": "rfc-ch04-c006",
      "order": 6,
      "title": "Hide Delegate",
      "teaser": "When clients navigate a chain of objects to reach what they need, give the first object in the chain a method that does the navigation internally.",
      "explanation": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Law of Demeter violations look like this: "
            },
            {
              "text": "order.getCustomer().getAddress().getCity()",
              "marks": [
                "code"
              ]
            },
            {
              "text": ". Every "
            },
            {
              "text": ".",
              "marks": [
                "code"
              ]
            },
            {
              "text": " after the first couples the caller to the internal structure of a chain of objects. When "
            },
            {
              "text": "Customer",
              "marks": [
                "code"
              ]
            },
            {
              "text": " restructures how it stores addresses, every caller that navigated the chain has to change."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Mechanical recipe:",
              "marks": [
                "bold"
              ]
            }
          ]
        },
        {
          "type": "list",
          "style": "numbered",
          "items": [
            [
              {
                "text": "Identify the delegation chain that clients are navigating."
              }
            ],
            [
              {
                "text": "For each method the client calls on the end of the chain, add a delegating method to the first object that calls through internally."
              }
            ],
            [
              {
                "text": "Update clients to call the new method on the first object rather than navigating the chain."
              }
            ],
            [
              {
                "text": "Run tests."
              }
            ],
            [
              {
                "text": "Consider whether the intermediate class in the chain is still needed by external callers. If not, hide or remove its accessor."
              }
            ]
          ]
        },
        {
          "type": "code",
          "language": "ts",
          "code": "// Before — callers reach through Order into Customer into Address\nclass OrderSummary {\n  cityLabel(order: Order): string {\n    return order.customer.address.city;\n  }\n}\n\n// After — Order exposes what callers need directly\nclass Order {\n  shippingCity(): string {\n    return this.customer.address.city;\n  }\n}\n\nclass OrderSummary {\n  cityLabel(order: Order): string {\n    return order.shippingCity();\n  }\n}"
        },
        {
          "type": "callout",
          "variant": "tip",
          "runs": [
            {
              "text": "Hide Delegate is most valuable at module or layer boundaries, where you want to decouple callers from internal structure. Within a single tightly coupled subsystem, the extra forwarding methods can be more noise than signal — apply judgment."
            }
          ]
        }
      ],
      "feynman": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Hide Delegate is like asking a hotel concierge to arrange your restaurant reservation instead of calling the restaurant yourself — you don't need to know which restaurant the concierge calls or what their booking system looks like."
            }
          ]
        }
      ]
    },
    {
      "id": "rfc-ch04-c007",
      "order": 7,
      "title": "Remove Middle Man",
      "teaser": "When a class spends most of its time forwarding calls to a delegate, remove the indirection and let clients call the delegate directly.",
      "explanation": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Hide Delegate and Remove Middle Man are inverses. Every time you add a delegating method to hide an internal object, you take on the maintenance cost of keeping that method in sync with the delegate. Once a class is mostly made of forwarding methods, the \"encapsulation\" it provides is just noise — callers are better served by accessing the delegate directly."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Mechanical recipe:",
              "marks": [
                "bold"
              ]
            }
          ]
        },
        {
          "type": "list",
          "style": "numbered",
          "items": [
            [
              {
                "text": "Create an accessor method on the middle class that exposes the delegate directly."
              }
            ],
            [
              {
                "text": "For each client that currently calls a forwarding method, update the call to navigate through the accessor and call the delegate directly."
              }
            ],
            [
              {
                "text": "After updating all clients for a given forwarding method, delete that forwarding method."
              }
            ],
            [
              {
                "text": "Repeat for each forwarding method until none remain."
              }
            ],
            [
              {
                "text": "Evaluate whether the accessor itself (or the middle class) still serves a purpose."
              }
            ]
          ]
        },
        {
          "type": "code",
          "language": "ts",
          "code": "// Before — Order has accumulated forwarding methods for Customer\nclass Order {\n  customerName(): string { return this.customer.name; }\n  customerEmail(): string { return this.customer.email; }\n  customerTier(): string { return this.customer.loyaltyTier; }\n}\n\n// After — expose the Customer and let callers use it directly\nclass Order {\n  get customer(): Customer { return this._customer; }\n}\n\n// Callers now: order.customer.name, order.customer.email, etc."
        },
        {
          "type": "callout",
          "variant": "info",
          "runs": [
            {
              "text": "The right balance between Hide Delegate and Remove Middle Man shifts over time. A class that wraps one concept in a stable API is worth keeping; a class that just passes calls through to an unstable internal object should be removed."
            }
          ]
        }
      ],
      "feynman": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Removing the middle man is like canceling a subscription to a news aggregator once you realize you only read articles from one source — cut out the step and go directly."
            }
          ]
        }
      ]
    },
    {
      "id": "rfc-ch04-c008",
      "order": 8,
      "title": "Introduce Foreign Method",
      "teaser": "When you need behavior on a class you cannot modify, add a standalone function or static helper that takes an instance of that class as its first argument.",
      "explanation": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Sometimes the class that should own a method is a third-party type, a framework class, or a generated type you have no control over. You cannot add the method directly. Introduce Foreign Method is the minimal workaround: write the method as a free function (or a static helper on the consuming class) that takes an instance of the foreign class as its first argument."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Mechanical recipe:",
              "marks": [
                "bold"
              ]
            }
          ]
        },
        {
          "type": "list",
          "style": "numbered",
          "items": [
            [
              {
                "text": "Write the method in the consuming class as a private static method (or a module-level function in Python/TypeScript). Pass an instance of the foreign class as the first parameter."
              }
            ],
            [
              {
                "text": "Add a comment noting that this is a foreign method and belongs on the target class if it were ever made modifiable."
              }
            ],
            [
              {
                "text": "Call it wherever the operation is needed, passing the foreign instance explicitly."
              }
            ],
            [
              {
                "text": "If you need this operation in more than two or three places, consider Introduce Local Extension instead."
              }
            ]
          ]
        },
        {
          "type": "code",
          "language": "ts",
          "code": "// Date is a built-in; you cannot add methods to it.\n// Before — date arithmetic scattered in callers\nfunction processDueDate(invoice: Invoice): void {\n  const due = new Date(invoice.issuedAt);\n  due.setDate(due.getDate() + invoice.netDays);\n  invoice.dueAt = due;\n}\n\n// After — extracted as a foreign method with an explanatory name\n// Foreign method: belongs on Date if Date were extensible\nfunction addDays(date: Date, days: number): Date {\n  const result = new Date(date);\n  result.setDate(result.getDate() + days);\n  return result;\n}\n\nfunction processDueDate(invoice: Invoice): void {\n  invoice.dueAt = addDays(invoice.issuedAt, invoice.netDays);\n}"
        },
        {
          "type": "callout",
          "variant": "warning",
          "runs": [
            {
              "text": "Introduce Foreign Method is a stopgap. If you find yourself writing many foreign methods for the same class, switch to Introduce Local Extension — a subclass or wrapper that adds those methods in one coherent place."
            }
          ]
        }
      ],
      "feynman": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "A foreign method is like writing a sticky note of instructions for a tool you cannot modify — it works, but it belongs on the tool itself and you're only keeping it nearby until a better solution appears."
            }
          ]
        }
      ]
    },
    {
      "id": "rfc-ch04-c009",
      "order": 9,
      "title": "Introduce Local Extension",
      "teaser": "When you need multiple methods on a class you cannot modify, create a subclass or wrapper that adds those methods and use it consistently throughout your codebase.",
      "explanation": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Where Introduce Foreign Method adds a single operation as a standalone helper, Introduce Local Extension is appropriate when you need several operations on a type you do not own. Rather than accumulating scattered foreign methods, you create a coherent extension point: either a subclass (if the type is non-final) or a wrapper class (if subclassing is blocked or unsuitable)."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Mechanical recipe:",
              "marks": [
                "bold"
              ]
            }
          ]
        },
        {
          "type": "list",
          "style": "numbered",
          "items": [
            [
              {
                "text": "Choose between subclass and wrapper. Use a subclass if it is straightforward to construct from an existing instance and the type is not final/sealed. Use a wrapper otherwise."
              }
            ],
            [
              {
                "text": "Create the extension class. For a subclass, add a constructor that delegates to the parent. For a wrapper, hold a reference to the wrapped instance and delegate all original methods."
              }
            ],
            [
              {
                "text": "Add the new methods to the extension class."
              }
            ],
            [
              {
                "text": "Update construction sites in the consuming code to produce the extension class instead of the original type."
              }
            ],
            [
              {
                "text": "Update type annotations and parameters where the extension features are needed — you may not need to update every site, only those that use the new methods."
              }
            ]
          ]
        },
        {
          "type": "code",
          "language": "ts",
          "code": "// MoneyAmount is a third-party value type with no business logic\nclass RichMoneyAmount extends MoneyAmount {\n  constructor(base: MoneyAmount) {\n    super(base.amount, base.currency);\n  }\n\n  applyDiscount(rate: number): RichMoneyAmount {\n    return new RichMoneyAmount(\n      new MoneyAmount(this.amount * (1 - rate), this.currency)\n    );\n  }\n\n  addTax(rate: number): RichMoneyAmount {\n    return new RichMoneyAmount(\n      new MoneyAmount(this.amount * (1 + rate), this.currency)\n    );\n  }\n}\n\nconst lineTotal = new RichMoneyAmount(invoice.subtotal)\n  .applyDiscount(0.1)\n  .addTax(0.08);"
        },
        {
          "type": "callout",
          "variant": "tip",
          "runs": [
            {
              "text": "Prefer the wrapper form when the original type might change its constructor signature, or when you want to present a narrower interface to callers — the wrapper lets you expose only what they need."
            }
          ]
        }
      ],
      "feynman": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "A local extension is like attaching a custom adapter to a standard power strip — you keep using the strip as-is, but your new adapter gives you the extra socket you needed without rewiring the original."
            }
          ]
        }
      ]
    },
    {
      "id": "rfc-ch04-c010",
      "order": 10,
      "title": "Encapsulate Field",
      "teaser": "When a class exposes a mutable field directly, hide it behind accessors so the class controls how its data is read and modified.",
      "explanation": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "A public field is a hole in encapsulation. Any caller can write any value at any time with no validation, no notification, no side effects. The class cannot defend its own invariants. Encapsulate Field is the refactoring that closes the hole."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Mechanical recipe:",
              "marks": [
                "bold"
              ]
            }
          ]
        },
        {
          "type": "list",
          "style": "numbered",
          "items": [
            [
              {
                "text": "Make the field private (or the narrowest access level that compiles)."
              }
            ],
            [
              {
                "text": "Add a getter method. Name it after the concept, not after the mechanical accessor convention if you can avoid it."
              }
            ],
            [
              {
                "text": "Add a setter method. Apply any validation logic the field needs."
              }
            ],
            [
              {
                "text": "Update all read and write sites throughout the codebase to use the accessor rather than the field directly. Compile after each change."
              }
            ],
            [
              {
                "text": "Review the setter: in many cases, once you have control of writes, you realize the setter should be removed entirely and the field set only through a meaningful business method like "
              },
              {
                "text": "applyDiscount()",
                "marks": [
                  "code"
                ]
              },
              {
                "text": " or "
              },
              {
                "text": "confirmShipment()",
                "marks": [
                  "code"
                ]
              },
              {
                "text": "."
              }
            ]
          ]
        },
        {
          "type": "code",
          "language": "ts",
          "code": "// Before — status can be set to anything by anyone\nclass Order {\n  status: string;\n}\norder.status = \"SHIPPED\"; // no validation, no side effects\n\n// After — Order controls transitions through its own interface\nclass Order {\n  private _status: OrderStatus = OrderStatus.Pending;\n\n  get status(): OrderStatus { return this._status; }\n\n  confirmShipment(trackingId: string): void {\n    if (this._status !== OrderStatus.Processing) {\n      throw new Error(\"Can only ship a processing order\");\n    }\n    this._status = OrderStatus.Shipped;\n    this.trackingId = trackingId;\n  }\n}"
        },
        {
          "type": "callout",
          "variant": "info",
          "runs": [
            {
              "text": "The real value of Encapsulate Field is not the getter and setter themselves — it is the opportunity to replace free-form assignment with meaningful business methods that enforce valid state transitions."
            }
          ]
        }
      ],
      "feynman": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Encapsulating a field is like replacing an unlocked filing cabinet with a front-desk clerk — the information is still accessible, but now there is a person who checks what you are doing before handing anything over."
            }
          ]
        }
      ]
    },
    {
      "id": "rfc-ch04-c011",
      "order": 11,
      "title": "Self-Encapsulate Field",
      "teaser": "Even inside a class, access your own fields through accessor methods so that subclasses and later changes have a single place to intercept reads and writes.",
      "explanation": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Self-Encapsulate Field goes one step further than Encapsulate Field: you use the accessor methods even from within the class itself, not just from outside callers. Direct field access inside a class bypasses the accessor logic and makes it harder to override behavior in subclasses."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Mechanical recipe:",
              "marks": [
                "bold"
              ]
            }
          ]
        },
        {
          "type": "list",
          "style": "numbered",
          "items": [
            [
              {
                "text": "Add private getter and setter methods for the field (as in Encapsulate Field)."
              }
            ],
            [
              {
                "text": "Search for all direct accesses to the field within the class body, including constructors, methods, and property initializers."
              }
            ],
            [
              {
                "text": "Replace each direct access with the corresponding getter or setter call."
              }
            ],
            [
              {
                "text": "The constructor is a special case: decide whether the constructor should bypass the setter (to avoid running incomplete initialization logic) or use it (if the setter validation is safe to run during construction)."
              }
            ],
            [
              {
                "text": "Run tests. The behavior should be identical — you have changed the access path, not the logic."
              }
            ]
          ]
        },
        {
          "type": "code",
          "language": "ts",
          "code": "// Before — Order reads its own discount field directly\nclass Order {\n  private discount: number = 0;\n\n  totalAfterDiscount(): number {\n    return this.subtotal * (1 - this.discount); // direct field access\n  }\n}\n\n// After — reads go through the accessor; subclass can override\nclass Order {\n  private _discount: number = 0;\n\n  protected get discount(): number { return this._discount; }\n  protected set discount(value: number) {\n    if (value < 0 || value > 1) throw new Error(\"Invalid discount\");\n    this._discount = value;\n  }\n\n  totalAfterDiscount(): number {\n    return this.subtotal * (1 - this.discount); // accessor call\n  }\n}"
        },
        {
          "type": "callout",
          "variant": "tip",
          "runs": [
            {
              "text": "Self-encapsulation is most valuable in class hierarchies where you expect subclasses to override the lazy initialization or caching strategy of a field. In a sealed class with no planned subclasses, the indirection may not be worth the noise."
            }
          ]
        }
      ],
      "feynman": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Using your own accessors inside a class is like a chef following the restaurant's own recipes even when cooking for themselves — consistency means any change to the recipe propagates everywhere, including the chef's own plate."
            }
          ]
        }
      ]
    },
    {
      "id": "rfc-ch04-c012",
      "order": 12,
      "title": "Replace Data Class with Data Behavior",
      "teaser": "When a class holds data but all its behavior lives in the classes that manipulate it, move that behavior back in — anemic domain models are feature envy made architectural.",
      "explanation": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "The anemic domain model is the structural form of feature envy. You have an "
            },
            {
              "text": "Invoice",
              "marks": [
                "code"
              ]
            },
            {
              "text": " class with a dozen fields and no methods, and a separate "
            },
            {
              "text": "InvoiceService",
              "marks": [
                "code"
              ]
            },
            {
              "text": " class with a dozen methods that do nothing but read and write "
            },
            {
              "text": "Invoice",
              "marks": [
                "code"
              ]
            },
            {
              "text": " fields. The service class is feature envy at scale — it exists solely because the "
            },
            {
              "text": "Invoice",
              "marks": [
                "code"
              ]
            },
            {
              "text": " class was kept deliberately thin."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "This is the refactoring that closes the chapter's loop: you started with the principle that behavior should live next to the data it uses, and a data class is the failure state where that principle was abandoned entirely."
            }
          ]
        },
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "Mechanical recipe:",
              "marks": [
                "bold"
              ]
            }
          ]
        },
        {
          "type": "list",
          "style": "numbered",
          "items": [
            [
              {
                "text": "Identify all the service/utility methods that operate primarily on the data class's own fields. These are the displaced behaviors."
              }
            ],
            [
              {
                "text": "Pick the most cohesive cluster — the methods that touch the fewest other objects and rely most heavily on the data class's own state."
              }
            ],
            [
              {
                "text": "Move those methods onto the data class using Move Method. Remove the now-redundant parameters that were previously passed in as the data object."
              }
            ],
            [
              {
                "text": "Evaluate which fields can be made private now that behavior lives alongside them — apply Encapsulate Field where appropriate."
              }
            ],
            [
              {
                "text": "Repeat for each cluster of behavior until the service class either disappears or is left with only genuinely cross-cutting operations that involve multiple domain objects."
              }
            ]
          ]
        },
        {
          "type": "code",
          "language": "ts",
          "code": "// Before — Invoice is a pure data carrier; all logic is elsewhere\nclass Invoice {\n  lineItems: LineItem[];\n  discountRate: number;\n  taxRate: number;\n  paidAt: Date | null;\n}\n\nclass InvoiceService {\n  subtotal(invoice: Invoice): number {\n    return invoice.lineItems.reduce((s, item) => s + item.total, 0);\n  }\n  amountDue(invoice: Invoice): number {\n    const sub = this.subtotal(invoice);\n    return sub * (1 - invoice.discountRate) * (1 + invoice.taxRate);\n  }\n  isPaid(invoice: Invoice): boolean {\n    return invoice.paidAt !== null;\n  }\n}\n\n// After — Invoice knows how to compute its own state\nclass Invoice {\n  private lineItems: LineItem[];\n  private discountRate: number;\n  private taxRate: number;\n  private paidAt: Date | null;\n\n  subtotal(): number {\n    return this.lineItems.reduce((s, item) => s + item.total, 0);\n  }\n  amountDue(): number {\n    return this.subtotal() * (1 - this.discountRate) * (1 + this.taxRate);\n  }\n  isPaid(): boolean {\n    return this.paidAt !== null;\n  }\n}"
        },
        {
          "type": "callout",
          "variant": "warning",
          "runs": [
            {
              "text": "Not every data class should absorb all the behavior that touches it. Methods involving multiple domain objects or infrastructure concerns (persistence, serialization, HTTP) belong in service layers. The criterion is: does this behavior depend only on this object's own state? If yes, it belongs here."
            }
          ]
        }
      ],
      "feynman": [
        {
          "type": "paragraph",
          "runs": [
            {
              "text": "A data class that gains its own behavior is like a recipe card that learns to cook — instead of waiting for someone else to read its instructions and act, it can prepare the dish itself."
            }
          ]
        }
      ]
    }
  ]
}