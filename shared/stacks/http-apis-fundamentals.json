{
  "stack": "http-apis-fundamentals",
  "words": [
    {
      "id": "http0010-0000-0000-0000-000000000001",
      "word": "MIME Type",
      "pronunciation": "MIME TYPE",
      "partOfSpeech": "noun",
      "shortDefinition": "A two-part identifier (`type/subtype`) telling clients and servers the format of a resource — `text/html`, `application/json`, `image/png`.",
      "simpleDefinition": "A label like `application/json` that tells the receiver how to interpret the bytes — text, JSON, image, video.",
      "longDefinition": "MIME types (now formally 'media types') are how HTTP describes the format of payloads. The `Content-Type` request/response header carries them. Browsers use them to decide how to render; APIs use them to negotiate format (`Accept: application/json`); servers use them to pick the right parser. The `+suffix` convention (e.g. `application/vnd.api+json`) layers schemas on top of base formats.",
      "techContext": "Common gotcha: missing or wrong Content-Type causes browsers to MIME-sniff, which breaks security headers (X-Content-Type-Options: nosniff is the fix). Always set Content-Type explicitly on API responses.",
      "exampleSentence": "Set the response Content-Type to `application/problem+json` so clients know to parse it as RFC 7807 errors.",
      "etymology": "Multipurpose Internet Mail Extensions — originally an email standard (RFC 1341, 1992) that HTTP adopted.",
      "connector": "Like a file extension that travels with the bytes — `.json` says 'parse as JSON,' `application/json` says the same over the wire.",
      "codeExample": {
        "language": "http",
        "code": "POST /api/orders HTTP/1.1\nHost: api.example.com\nContent-Type: application/json\nAccept: application/json\n\n{\"productId\": 42, \"qty\": 2}\n\nHTTP/1.1 201 Created\nContent-Type: application/json; charset=utf-8\nX-Content-Type-Options: nosniff\n\n{\"id\": \"ord_abc123\"}"
      },
      "unlockLevel": 1,
      "tags": [
        "http",
        "headers"
      ],
      "category": "concepts"
    },
    {
      "id": "http0020-0000-0000-0000-000000000002",
      "word": "REST",
      "pronunciation": "REST",
      "partOfSpeech": "noun",
      "shortDefinition": "An architectural style for APIs that uses HTTP methods and resource-based URLs.",
      "simpleDefinition": "Architectural style for web APIs where every piece of data has a unique address — enabling interaction through standard HTTP verbs like GET, POST, and DELETE. Emphasizes statelessness and a uniform interface for scalable web services.",
      "longDefinition": "Representational State Transfer defines six constraints — statelessness, uniform interface, client-server separation, cacheability, layered system, and optional code on demand. A RESTful API maps CRUD operations to HTTP methods (GET, POST, PUT, DELETE) on noun-based endpoints.",
      "connector": "Think of REST like a filing cabinet — each folder has a fixed location and you use standard actions to manage the contents.",
      "techContext": "REST is the dominant API style for web services. Its stateless nature makes it horizontally scalable by default.",
      "exampleSentence": "We designed the API following REST conventions so each resource had a predictable URL and the correct HTTP method.",
      "etymology": "coined by Roy Fielding in his 2000 PhD dissertation at UC Irvine.",
      "codeExample": {
        "language": "http",
        "code": "GET    /articles        # list\nPOST   /articles        # create\nGET    /articles/42     # read\nPUT    /articles/42     # update\nDELETE /articles/42     # delete"
      },
      "unlockLevel": 1,
      "tags": [
        "web",
        "api",
        "architecture"
      ],
      "category": "concepts"
    },
    {
      "id": "http0030-0000-0000-0000-000000000003",
      "word": "Listener",
      "pronunciation": "LIS-ner",
      "partOfSpeech": "noun",
      "shortDefinition": "A server process or socket bound to a port and waiting to accept incoming connections.",
      "simpleDefinition": "A bound socket on a port — accepts new TCP connections and hands them to the application. The 'I am open for traffic' part of any web service.",
      "longDefinition": "Every HTTP server starts with a listener: a socket bound to a host:port pair, accepting connections. Frameworks hide it behind `app.listen(3000)` calls, but underneath it's `bind() + listen() + accept()`. Multiple listeners on different ports = multiple ways into your service.",
      "techContext": "Reverse proxies and load balancers manage their own listeners. Common ops question: 'is the service listening?' is answered by `ss -lntp` or `lsof -i :PORT` — both check the kernel's listener table.",
      "exampleSentence": "Add a second listener on 8443 for TLS — keep 8080 for the HTTP-to-HTTPS redirect.",
      "etymology": "Plain English — a process that listens for connections.",
      "connector": "Like an event listener in JavaScript — bind once, fire the handler whenever the event arrives.",
      "codeExample": {
        "language": "go",
        "code": "srv := &http.Server{\n    Addr:    \":8080\",\n    Handler: mux,\n}\n\n// Listener is what makes the server \"live\"\nlog.Fatal(srv.ListenAndServe())\n\n// Manually:\nln, _ := net.Listen(\"tcp\", \":8080\")\nfor {\n    conn, _ := ln.Accept()    // blocks until a client connects\n    go handle(conn)\n}"
      },
      "unlockLevel": 1,
      "tags": [
        "http",
        "networking"
      ],
      "category": "concepts"
    },
    {
      "id": "http0040-0000-0000-0000-000000000004",
      "word": "CRUD",
      "pronunciation": "KRUD",
      "partOfSpeech": "acronym",
      "shortDefinition": "Foundational operations for persistent storage: Create, Read, Update, and Delete.",
      "simpleDefinition": "The four foundational operations for persistent data management — Create, Read, Update, and Delete. These fundamental actions form the basis of most resource manipulation in RESTful APIs, typically mapping to POST, GET, PUT, and DELETE.",
      "longDefinition": "CRUD represents the four essential actions required to manage data within a persistent storage system. In the context of API design, these operations typically align with specific HTTP methods: POST for creation, GET for reading, PUT or PATCH for updates, and DELETE for removal. Most resource-oriented APIs are built upon this pattern, providing a predictable and standardized way for developers to interact with database records through a clean, abstracted interface across the network.",
      "connector": "Think of CRUD like managing a contact list — you add a name, view details, edit the number, or delete the entry.",
      "techContext": "I spent the morning implementing the CRUD operations for our new 'Project' resource.",
      "exampleSentence": "Our admin dashboard provides a simple CRUD interface for managing user accounts.",
      "etymology": "Acronym for Create, Read, Update, Delete.",
      "codeExample": {
        "language": "http",
        "code": "POST /users (Create)\nGET /users/1 (Read)\nPUT /users/1 (Update)\nDELETE /users/1 (Delete)"
      },
      "unlockLevel": 1,
      "tags": [
        "api",
        "databases",
        "fundamentals"
      ],
      "category": "concepts"
    },
    {
      "id": "http0050-0000-0000-0000-000000000005",
      "word": "API Contract",
      "pronunciation": "AY-pee-eye KON-trakt",
      "partOfSpeech": "noun",
      "shortDefinition": "The agreement — explicit or implicit — between an API and its consumers about endpoints, payloads, status codes, and behavior.",
      "simpleDefinition": "Everything callers depend on: URLs, request/response shapes, error codes, headers. Break any of it and clients break.",
      "longDefinition": "The API contract is the surface area you can't change without coordinating with consumers. It includes obvious things (endpoint paths, JSON schemas) and subtle ones (error message wording, header presence, response time SLA). OpenAPI documents the structural contract; written SLAs and changelogs cover the rest.",
      "techContext": "Contract testing tools (Pact, Spring Cloud Contract) let consumers and producers agree on a contract and verify it on every CI run — catching breaking changes before they ship. Versioning strategy (URL versioning, header versioning, content negotiation) is how you evolve the contract over time.",
      "exampleSentence": "Adding the new field is fine — but renaming `customer_id` to `userId` breaks the API contract; ship it under v2.",
      "etymology": "API + contract (from Latin contractus — agreement).",
      "connector": "Like a TypeScript interface that crosses the network boundary — a promise about shape, name, and behavior.",
      "codeExample": {
        "language": "yaml",
        "code": "# OpenAPI snippet — the structural contract\npaths:\n  /orders/{id}:\n    get:\n      parameters:\n        - { name: id, in: path, required: true, schema: { type: string } }\n      responses:\n        '200':\n          content:\n            application/json:\n              schema: { $ref: '#/components/schemas/Order' }\n        '404':\n          description: Order not found"
      },
      "unlockLevel": 1,
      "tags": [
        "http",
        "api-design"
      ],
      "category": "concepts"
    },
    {
      "id": "http0060-0000-0000-0000-000000000006",
      "word": "Keep-Alive",
      "pronunciation": "KEEP-uh-live",
      "partOfSpeech": "noun",
      "shortDefinition": "An HTTP feature that reuses the same TCP connection for multiple requests instead of opening a new one each time.",
      "simpleDefinition": "Reuse the same TCP/TLS connection across multiple HTTP requests instead of paying the handshake cost every time. Default in HTTP/1.1+.",
      "longDefinition": "Keep-alive (a.k.a. persistent connections) was HTTP/1.1's killer perf feature — TCP and TLS handshakes are expensive and a fresh connection per request is wasteful. Modern servers keep connections open for a configurable timeout, reusing them for subsequent requests from the same client.",
      "techContext": "HTTP/2 takes this further with multiplexing — many concurrent requests over one connection. HTTP/3 changes the transport (QUIC over UDP) but the principle holds. Keep-alive timeout is a tradeoff: longer = fewer handshakes, more idle connections; shorter = more handshakes, faster server resource recycling.",
      "exampleSentence": "Bump the keep-alive timeout from 5s to 30s — clients are reconnecting too often and we're paying the TLS overhead.",
      "etymology": "Plain English — keeps the connection alive between requests.",
      "connector": "Like database connection pooling — reuse the expensive setup, amortize over many cheap operations.",
      "codeExample": {
        "language": "http",
        "code": "GET /api/items HTTP/1.1\nHost: api.example.com\nConnection: keep-alive\n\nHTTP/1.1 200 OK\nContent-Length: 142\nConnection: keep-alive\nKeep-Alive: timeout=30, max=100\n\n[ ... ]\n\n# Client can now send another request on the same connection"
      },
      "unlockLevel": 1,
      "tags": [
        "http",
        "performance"
      ],
      "category": "concepts"
    },
    {
      "id": "http0070-0000-0000-0000-000000000007",
      "word": "Swagger",
      "pronunciation": "SWAG-ur",
      "partOfSpeech": "noun",
      "shortDefinition": "Development tool suite using OpenAPI for documentation and client generation.",
      "simpleDefinition": "Popular suite of development tools built around the OpenAPI Specification — including UI for interactive documentation and codegen for client libraries. Provides a visual playground for developers to explore and test endpoints in real-time.",
      "longDefinition": "Swagger is the most widely used ecosystem for designing, building, and documenting RESTful APIs. It includes the Swagger UI, which renders OpenAPI specifications as interactive documentation, allowing developers to test endpoints directly from their browser. Additionally, Swagger Codegen can automatically produce client libraries in dozens of languages, drastically speeding up the integration process. By using Swagger, teams can provide a high-quality developer experience that remains synchronized with the actual implementation of the service throughout its entire lifecycle.",
      "connector": "Think of Swagger like an interactive car brochure — you can push every button and test features before you even take a drive.",
      "techContext": "You can test the new endpoints directly in our Swagger UI without writing any code.",
      "exampleSentence": "I used Swagger to generate a TypeScript client for our backend API in seconds.",
      "etymology": "Invented name for the toolset, possibly implying confidence/style.",
      "codeExample": {
        "language": "json",
        "code": "{\n  \"swagger\": \"2.0\",\n  \"info\": { \"title\": \"My API\", \"version\": \"1.0.0\" }\n}"
      },
      "unlockLevel": 1,
      "tags": [
        "api",
        "documentation",
        "tools"
      ],
      "category": "concepts"
    },
    {
      "id": "http0080-0000-0000-0000-000000000008",
      "word": "HMAC",
      "pronunciation": "AYCH-mak",
      "partOfSpeech": "noun",
      "shortDefinition": "Hash-based Message Authentication Code — a cryptographic signature combining a secret key with a hash of the message, used to verify authenticity and integrity.",
      "simpleDefinition": "A signature that proves a request came from someone who knows the shared secret AND that the message wasn't tampered with. Stripe, AWS, GitHub webhooks all use it.",
      "longDefinition": "HMAC binds a message to a secret key via a hash function. The sender computes `HMAC(key, message)` and includes the digest as a signature header. The receiver, sharing the key, recomputes and compares. Tamper with the message in transit and the signature mismatches; lack the key and you can't forge a signature.",
      "techContext": "HMAC-SHA256 is the modern default. The pattern beats raw API keys for webhooks because the receiver doesn't need to trust the network — even an attacker who reads the secret-bearing requests in flight can't forge new ones. Always use a constant-time comparison to avoid timing attacks.",
      "exampleSentence": "Switch the webhook auth from bearer tokens to HMAC-signed payloads so we can publish receivers behind a CDN safely.",
      "etymology": "Hash-based Message Authentication Code (RFC 2104, 1997).",
      "connector": "Like signing every request with your private key — proves identity AND integrity in one step.",
      "codeExample": {
        "language": "python",
        "code": "import hmac, hashlib\n\nsecret = b'shared_webhook_secret'\nbody = request.body  # raw bytes\nreceived_sig = request.headers['X-Signature']\n\nexpected = hmac.new(secret, body, hashlib.sha256).hexdigest()\n\n# Constant-time compare\nif not hmac.compare_digest(expected, received_sig):\n    abort(401)"
      },
      "unlockLevel": 1,
      "tags": [
        "security",
        "auth"
      ],
      "category": "concepts"
    },
    {
      "id": "http0090-0000-0000-0000-000000000009",
      "word": "Middleware",
      "pronunciation": "MID-ul-wair",
      "partOfSpeech": "noun",
      "shortDefinition": "Functions executed sequentially during the request-response cycle to handle cross-cutting application concerns.",
      "simpleDefinition": "Functions executed sequentially within a web server's request-response cycle — providing a modular way to handle logging, authentication, and validation. They can modify request objects, terminate responses, or pass control to the next handler.",
      "longDefinition": "Middleware functions are the building blocks of modern Node.js web frameworks like Express and Koa. They sit between the incoming network request and the final route handler, allowing developers to inject logic that applies to multiple routes. This pattern is ideal for handling security headers, parsing request bodies, managing sessions, or logging activity. By chaining these functions together, you create a pipeline where each step can either augment the data or stop the process if errors occur.",
      "connector": "Think of middleware like airport security — every passenger (request) passes through multiple checkpoints before they are allowed to reach their gate.",
      "techContext": "Used for cross-cutting concerns like logging, authentication, parsing JSON, or handling errors.",
      "exampleSentence": "We added a middleware function to our Express app to verify the JWT token before allowing access to the protected API routes.",
      "codeExample": {
        "language": "js",
        "code": "app.use((req, res, next) => {\n  console.log(`${req.method} request to ${req.url}`);\n  next(); // Pass control to the next function\n});"
      },
      "etymology": "from 'middle' + 'software'.",
      "unlockLevel": 2,
      "tags": [
        "nodejs",
        "web-server",
        "express",
        "patterns"
      ],
      "category": "concepts"
    },
    {
      "id": "http0100-0000-0000-0000-000000000010",
      "word": "Pagination",
      "pronunciation": "paj-ih-NAY-shun",
      "partOfSpeech": "noun",
      "shortDefinition": "Dividing large datasets into discrete chunks or pages for efficient retrieval.",
      "simpleDefinition": "Dividing large datasets into smaller, discrete chunks or pages — prevents overwhelming the client and server with massive payloads. Common strategies include offset-based and cursor-based approaches to ensure efficient, manageable data retrieval across network boundaries.",
      "longDefinition": "Pagination is essential for managing performance when dealing with large resource collections. Offset-based pagination uses 'limit' and 'offset' but can become slow as the dataset grows because the database must skip many rows. Cursor-based pagination provides better performance for large sets by using a pointer to a specific record. Both methods help reduce memory usage and network latency by ensuring clients only receive manageable slices of data at any given time.",
      "connector": "Think of pagination like chapters in a book — you turn to one chapter at a time rather than trying to read every page simultaneously.",
      "techContext": "We switched from offset pagination to cursor-based pagination to handle our billion-row activity feed more efficiently.",
      "exampleSentence": "The API response included a 'next_page_url' to help the client handle pagination automatically.",
      "etymology": "from Latin pagina ('page, leaf of paper').",
      "codeExample": {
        "language": "http",
        "code": "GET /v1/users?limit=20&offset=40\nGET /v1/orders?cursor=Ym9va21hcmsxMjM="
      },
      "unlockLevel": 2,
      "tags": [
        "api",
        "performance",
        "data-handling"
      ],
      "category": "processes"
    },
    {
      "id": "http0110-0000-0000-0000-000000000011",
      "word": "Versioning",
      "pronunciation": "VUR-zhun-ing",
      "partOfSpeech": "noun",
      "shortDefinition": "Managing changes to an API to ensure compatibility for existing clients.",
      "simpleDefinition": "The practice of labeling different iterations of an API — ensures backward compatibility while introducing breaking changes. This allows existing clients to continue using older logic while new developers adopt the latest features and data schemas.",
      "longDefinition": "API versioning allows developers to introduce breaking changes without disrupting current users. Common strategies include URI versioning, such as adding /v1/ to the path, or header-based versioning using the 'Accept' or custom 'X-API-Version' header. Effective versioning policies clearly communicate deprecation timelines and provide a migration path, ensuring the ecosystem remains stable while the service evolves its data models, authentication methods, or response structures to meet new requirements.",
      "connector": "Think of versioning like software editions — Word 2019 and 2023 coexist, ensuring older documents don't break when a new version is released.",
      "techContext": "We released 'v2' of our API to support the new JSON schema, but we'll continue to support 'v1' for at least another year.",
      "exampleSentence": "API versioning is essential for maintaining backward compatibility while the product evolves.",
      "etymology": "from Latin versio ('a turning').",
      "codeExample": {
        "language": "http",
        "code": "GET /v1/users/123 HTTP/1.1\nHost: api.example.com"
      },
      "unlockLevel": 2,
      "tags": [
        "api",
        "lifecycle",
        "management"
      ],
      "category": "processes"
    },
    {
      "id": "http0120-0000-0000-0000-000000000012",
      "word": "OpenAPI",
      "pronunciation": "OH-pun-AY-pee-eye",
      "partOfSpeech": "noun",
      "shortDefinition": "Standard format for defining and documenting RESTful API structures and endpoints.",
      "simpleDefinition": "Standard, machine-readable format for describing a REST API's entire structure — including endpoints, request parameters, and response schemas. Serves as the source of truth for generating documentation, client libraries, and mock servers automatically.",
      "longDefinition": "The OpenAPI Specification (OAS) provides a vendor-neutral description of an API's capabilities. Written in YAML or JSON, it outlines every endpoint, required parameters, authentication methods, and expected status codes. This 'API-as-code' approach enables a rich ecosystem of tools for automatic documentation generation, client-side SDK creation, and automated contract testing. By maintaining a central specification, teams can ensure that the frontend and backend remain synchronized throughout the development lifecycle, reducing integration friction and bugs.",
      "connector": "Think of OpenAPI like a building's architectural blueprints — they let contractors and clients understand the structure without walking through every room.",
      "techContext": "I updated the OpenAPI specification to include the new error response codes for our payment service.",
      "exampleSentence": "By maintaining an accurate OpenAPI spec, we can automatically generate our developer portal and interactive API console.",
      "etymology": "from 'Open' + 'API'.",
      "codeExample": {
        "language": "yaml",
        "code": "paths:\n  /users:\n    get:\n      summary: List all users\n      responses:\n        '200':\n          description: Success"
      },
      "unlockLevel": 2,
      "tags": [
        "api",
        "documentation",
        "standards"
      ],
      "category": "concepts"
    },
    {
      "id": "http0130-0000-0000-0000-000000000013",
      "word": "Chunked Transfer",
      "pronunciation": "CHUNKT TRANS-fur",
      "partOfSpeech": "noun",
      "shortDefinition": "An HTTP/1.1 transfer encoding that streams a response in size-prefixed chunks without a known total length.",
      "simpleDefinition": "Send a response in pieces (`Transfer-Encoding: chunked`) — each chunk has its own size header, the stream ends with a zero-length chunk. Useful when you don't know total size up front.",
      "longDefinition": "Chunked transfer encoding lets HTTP/1.1 send streaming responses without precomputing Content-Length. The body is a sequence of chunks, each prefixed with its hex-encoded size; a `0\\r\\n\\r\\n` terminator ends the stream. Server-Sent Events, large file downloads, and on-the-fly compression all rely on it.",
      "techContext": "HTTP/2 dropped chunked encoding (HTTP/2 has its own framing layer). For HTTP/1.1, watch out for proxies and middleware that buffer chunks — they defeat the streaming benefit.",
      "exampleSentence": "Stream the large CSV with chunked transfer so the client can start parsing while the server is still computing rows.",
      "etymology": "Standard HTTP/1.1 terminology — body delivered in chunks.",
      "connector": "Like a generator function on the wire — yield bytes as they're produced, no need to know the total ahead of time.",
      "codeExample": {
        "language": "http",
        "code": "HTTP/1.1 200 OK\nContent-Type: text/event-stream\nTransfer-Encoding: chunked\n\n7\\r\\n\nMozilla\\r\\n\n9\\r\\n\nDeveloper\\r\\n\n7\\r\\n\nNetwork\\r\\n\n0\\r\\n\n\\r\\n\n# Empty chunk = end of stream"
      },
      "unlockLevel": 2,
      "tags": [
        "http",
        "streaming"
      ],
      "category": "concepts"
    },
    {
      "id": "http0140-0000-0000-0000-000000000014",
      "word": "Webhooks",
      "pronunciation": "WEB-hooks",
      "partOfSpeech": "noun",
      "shortDefinition": "Automated HTTP callbacks triggered by events in one application to notify another.",
      "simpleDefinition": "Automated HTTP POST requests sent from one system to another when a specific event occurs — like a code push or payment. They enable real-time integration without the resource-heavy overhead of constant polling.",
      "longDefinition": "Webhooks provide a way for applications to send real-time data to other applications. Unlike APIs where you poll for data, webhooks are 'push' mechanisms triggered by events. When an event happens (e.g., a GitHub commit), the source site makes an HTTP request to the URL configured for the webhook. This is essential for CI/CD systems to start pipelines immediately upon detecting repository changes.",
      "connector": "Think of webhooks like a doorbell — instead of you checking the porch every minute, the visitor notifies you exactly when they arrive.",
      "techContext": "We set up a GitHub webhook to trigger our CI pipeline every time a developer pushes code to the repository.",
      "exampleSentence": "We configured a GitHub webhook to notify our Slack channel every time a production deployment succeeds or fails.",
      "codeExample": {
        "language": "js",
        "code": "app.post('/webhook', (req, res) => {\n  const { event, data } = req.body;\n  if (event === 'push') runBuild(data);\n  res.status(200).send('Event received');\n});"
      },
      "etymology": "from 'Web' + 'hook' (interceptor).",
      "unlockLevel": 2,
      "tags": [
        "cicd",
        "integration",
        "automation"
      ],
      "category": "concepts"
    },
    {
      "id": "http0150-0000-0000-0000-000000000015",
      "word": "OAuth Flow",
      "pronunciation": "OH-awth FLOH",
      "partOfSpeech": "noun",
      "shortDefinition": "A multi-step protocol exchange that issues access tokens to a third-party app on behalf of a user.",
      "simpleDefinition": "The redirect dance: user clicks 'Sign in with X', X authenticates them, X sends a code back, your app exchanges the code for an access token. That whole sequence is an OAuth flow.",
      "longDefinition": "OAuth 2.0 defines several flows for different client types. The Authorization Code flow (with PKCE for public clients) is the modern default — user is redirected to the provider, consents, comes back with a short-lived code, and the app exchanges that code for tokens server-side. Implicit and Resource Owner Password flows are deprecated.",
      "techContext": "PKCE (Proof Key for Code Exchange, RFC 7636) is now mandatory for public clients (mobile, SPA). It prevents intercepted authorization codes from being exchanged by attackers. OAuth 2.1 (in draft) consolidates current best practices.",
      "exampleSentence": "Replace the implicit flow with Authorization Code + PKCE — the auth team flagged the old flow in last quarter's audit.",
      "etymology": "OAuth = Open Authorization (the protocol family); flow refers to the sequence of HTTP exchanges.",
      "connector": "Like a multi-step handshake protocol — but for delegated authentication, with redirects and consent screens.",
      "codeExample": {
        "language": "text",
        "code": "1. App  →  /authorize?response_type=code&client_id=X&code_challenge=...&redirect_uri=...\n2. User authenticates with provider, consents\n3. Provider  →  app redirect_uri  ?code=AUTH_CODE\n4. App  →  /token  client_id=X, code_verifier=..., code=AUTH_CODE\n5. Provider  →  { \"access_token\": \"...\", \"refresh_token\": \"...\", \"expires_in\": 3600 }"
      },
      "unlockLevel": 2,
      "tags": [
        "http",
        "auth"
      ],
      "category": "concepts"
    },
    {
      "id": "http0160-0000-0000-0000-000000000016",
      "word": "Etag",
      "pronunciation": "EE-tag",
      "partOfSpeech": "noun",
      "shortDefinition": "An HTTP response header carrying an opaque identifier for a specific version of a resource — used for cache validation and conditional requests.",
      "simpleDefinition": "A version string the server returns (`ETag: \"v3-abc\"`); the client sends it back with `If-None-Match` to ask 'still the same?' — server replies 304 if so, full body if not.",
      "longDefinition": "ETags solve two problems efficiently: cache validation (don't re-download unchanged content) and optimistic concurrency (don't overwrite a record changed by someone else since you read it). Strong ETags imply byte-equal content; weak ETags (`W/\"...\"`) imply semantic equivalence.",
      "techContext": "Pair `ETag` with `If-None-Match` for GETs (conditional fetch) and with `If-Match` for PUTs (conditional update). For mutations, ETag-based concurrency is the standard alternative to locking.",
      "exampleSentence": "Return ETag on GET /document/{id}, require If-Match on PUT — clients trying to write a stale version get 412.",
      "etymology": "Entity Tag — from HTTP/1.1 (RFC 2616).",
      "connector": "Like a content hash — change the content, change the tag; same tag = same content.",
      "codeExample": {
        "language": "http",
        "code": "# First request\nGET /article/42 HTTP/1.1\n\nHTTP/1.1 200 OK\nETag: \"abc123\"\n\n# Subsequent request\nGET /article/42 HTTP/1.1\nIf-None-Match: \"abc123\"\n\nHTTP/1.1 304 Not Modified    # No body, save bandwidth\n\n# Optimistic update\nPUT /article/42 HTTP/1.1\nIf-Match: \"abc123\"\n\nHTTP/1.1 412 Precondition Failed   # Someone else updated since"
      },
      "unlockLevel": 2,
      "tags": [
        "http",
        "caching"
      ],
      "category": "concepts"
    },
    {
      "id": "http0170-0000-0000-0000-000000000017",
      "word": "GraphQL",
      "pronunciation": "GRAF-Q-L",
      "partOfSpeech": "noun",
      "shortDefinition": "A query language for APIs that lets clients request exactly the data they need.",
      "simpleDefinition": "API query language and runtime allowing clients to request exactly the specific data fields they need — avoiding over-fetching and under-fetching. It provides a strongly typed schema that defines the shape and relationships of available backend data.",
      "longDefinition": "GraphQL provides a strongly typed schema defining available queries and mutations. Clients specify which fields to retrieve, eliminating over-fetching and under-fetching. A single endpoint handles all queries. Developed by Facebook; open-sourced in 2015.",
      "connector": "Think of GraphQL like a custom sandwich order — you specify exactly which ingredients you want layer by layer for one plate.",
      "techContext": "Contrasts with REST where each endpoint returns a fixed structure. GraphQL introduces complexity (caching, N+1 queries) but offers flexibility.",
      "exampleSentence": "We adopted GraphQL so the mobile app could fetch user, posts, and comments in a single request instead of hitting three REST endpoints.",
      "etymology": "Graph (data structure) + QL (Query Language); released by Facebook in 2015.",
      "codeExample": {
        "language": "graphql",
        "code": "# Query:\nquery {\n  user(id: \"42\") {\n    name\n    email\n    posts {\n      title\n      comments { author }\n    }\n  }\n}\n\n# Client requests only needed fields"
      },
      "unlockLevel": 3,
      "tags": [
        "web",
        "api",
        "query-language"
      ],
      "category": "components"
    },
    {
      "id": "http0180-0000-0000-0000-000000000018",
      "word": "Statelessness",
      "pronunciation": "STAYT-less-ness",
      "partOfSpeech": "noun",
      "shortDefinition": "Principle where each request contains all information needed for processing.",
      "simpleDefinition": "Core architectural principle where the server does not store client context between requests — each call must contain all necessary data to be self-sufficient. Simplifies horizontal scaling by ensuring any server can handle any request.",
      "longDefinition": "Statelessness is a fundamental constraint of REST that improves scalability and reliability. Since the server does not retain session data between requests, any instance in a load-balanced cluster can process any incoming message. The client is responsible for including all required context, such as authentication tokens or session IDs. This reduces server-side complexity and memory overhead, making it significantly easier to recover from failures and distribute traffic across geographically dispersed data centers.",
      "connector": "Think of statelessness like a vending machine — it doesn't remember who used it last; every purchase requires a full selection and payment.",
      "techContext": "Our API's statelessness allows us to spin up dozens of new instances behind a load balancer without worrying about session affinity.",
      "exampleSentence": "Because our API is stateless, the client must include the authentication token in every single request.",
      "etymology": "from 'state' + '-less'.",
      "codeExample": {
        "language": "http",
        "code": "GET /api/profile HTTP/1.1\nAuthorization: Bearer <token_with_session_info>"
      },
      "unlockLevel": 3,
      "tags": [
        "api",
        "architecture",
        "rest"
      ],
      "category": "qualities"
    },
    {
      "id": "http0190-0000-0000-0000-000000000019",
      "word": "API Gateway",
      "pronunciation": "AY-pee-eye GAYT-way",
      "partOfSpeech": "noun",
      "shortDefinition": "Managed service for creating, publishing, and securing APIs at any scale.",
      "simpleDefinition": "Front door for applications — manage, secure, and scale APIs that connect to backend services like Lambda or EC2. It handles thousands of concurrent calls, providing traffic management, authorization, and throttling to protect your internal infrastructure.",
      "longDefinition": "Amazon API Gateway is a fully managed service that makes it easy for developers to create, publish, maintain, monitor, and secure APIs at any scale. It acts as a \"front door\" for applications to access data, business logic, or functionality from your backend services. API Gateway handles all the tasks involved in accepting and processing up to hundreds of thousands of concurrent API calls.",
      "connector": "Think of API Gateway like a building's receptionist — they check your ID, take your message, and direct you to the right office.",
      "techContext": "We used API Gateway to expose our Lambda functions as a RESTful API for our mobile app.",
      "exampleSentence": "We exposed our backend microservices through API Gateway to implement rate limiting and request validation for our public-facing mobile application.",
      "codeExample": {
        "language": "hcl",
        "code": "resource \"aws_api_gateway_rest_api\" \"MyDemoAPI\" {\n  name        = \"MyDemoAPI\"\n  description = \"This is my API for demonstration purposes\"\n}"
      },
      "etymology": "from 'API' + 'gateway' (an entrance).",
      "unlockLevel": 3,
      "tags": [
        "aws",
        "cloud",
        "api",
        "serverless"
      ],
      "category": "concepts"
    },
    {
      "id": "http0200-0000-0000-0000-000000000020",
      "word": "gRPC",
      "pronunciation": "JEE-ar-pee-see",
      "partOfSpeech": "noun",
      "shortDefinition": "High-performance RPC framework using Protocol Buffers and HTTP/2 for microservices.",
      "simpleDefinition": "High-performance framework using Protocol Buffers and HTTP/2 for efficient, type-safe Remote Procedure Calls — prioritizes low-latency and bi-directional streaming. Ideal for internal microservice communication where speed and contract-first development are critical engineering requirements.",
      "longDefinition": "Originally developed by Google, gRPC uses Protocol Buffers as a language-neutral mechanism for serializing structured data. By leveraging HTTP/2, it supports advanced features like multiplexing and bi-directional streaming, which are not easily achieved with traditional REST. Its contract-first approach forces developers to define service interfaces strictly, leading to fewer runtime errors. This makes gRPC the preferred choice for high-performance internal microservices where low latency and high throughput are the primary goals.",
      "connector": "Think of gRPC like a specialized intercom system between offices — faster and more structured than shouting down a corridor, but requiring matching equipment.",
      "techContext": "We used gRPC for communication between our internal services because it is much faster and more type-safe than REST.",
      "exampleSentence": "The gRPC service uses bi-directional streaming to provide real-time updates to the dashboard.",
      "etymology": "from 'g' (originally Google) + 'RPC' (Remote Procedure Call).",
      "codeExample": {
        "language": "protobuf",
        "code": "service UserService {\n  rpc GetUser (UserRequest) returns (UserResponse) {}\n}"
      },
      "unlockLevel": 4,
      "tags": [
        "api",
        "microservices",
        "performance"
      ],
      "category": "concepts"
    },
    {
      "id": "http0210-0000-0000-0000-000000000021",
      "word": "Idempotency",
      "pronunciation": "eye-dem-POH-ten-see",
      "partOfSpeech": "noun",
      "shortDefinition": "System property ensuring duplicate operations yield the same result without unintended side effects.",
      "simpleDefinition": "API property ensuring that making the same request multiple times has the same effect as making it once — preventing duplicate actions like charging a credit card twice. Critical for building resilient distributed systems.",
      "longDefinition": "Idempotency is a design guarantee that performing the same operation multiple times results in the same system state as performing it once. In distributed middleware, network failures often cause producers to retry messages, which can lead to duplicate processing. By implementing idempotency keys or unique transaction IDs, the consumer can recognize previously processed messages and skip redundant actions. This ensures that operations like database updates or payment processing remain consistent even during unstable network conditions.",
      "connector": "Think of idempotency like an elevator button — pressing it once or ten times has the same result: the elevator comes to your floor.",
      "techContext": "Using a unique transaction ID in the message payload allows our billing service to maintain idempotency, ensuring a customer isn't charged twice for the same order.",
      "exampleSentence": "We implemented idempotency in our worker service to handle duplicate messages that occasionally occur during network partitions.",
      "etymology": "From Latin 'idem' (same) and 'potentia' (power).",
      "codeExample": {
        "language": "javascript",
        "code": "if (!db.transactions.has(msg.id)) {\n  processTransaction(msg);\n  db.transactions.add(msg.id);\n}"
      },
      "unlockLevel": 4,
      "tags": [
        "reliability",
        "logic",
        "distributed-systems"
      ],
      "category": "components"
    },
    {
      "id": "http0220-0000-0000-0000-000000000022",
      "word": "Field Selection",
      "pronunciation": "FEELD suh-LEK-shun",
      "partOfSpeech": "noun",
      "shortDefinition": "Allowing clients to request only specific fields from an API resource.",
      "simpleDefinition": "Mechanism where clients explicitly list the specific resource fields they want in the response — often called sparse fieldsets. Drastically reduces payload size and over-fetching, which is especially beneficial for mobile users on constrained networks.",
      "longDefinition": "Field selection, or sparse fieldsets, empowers clients to customize the shape of the response they receive. By requesting only the necessary attributes—such as ?fields=id,name—clients can significantly reduce the amount of data transferred. This optimization is vital for mobile applications operating on slow or expensive data plans. On the server side, field selection can also reduce database load and serialization time by avoiding the processing of large or computed fields that the client doesn't actually need.",
      "connector": "Think of field selection like a café menu where you tick only the sides you want, getting exactly what you ordered.",
      "techContext": "We implemented field selection (?fields=id,name) to avoid sending large 'bio' and 'avatar' fields when only basic info is needed for a list view.",
      "exampleSentence": "Field selection helped us reduce our mobile app's average data usage by 30%.",
      "etymology": "from 'field' + 'selection'.",
      "codeExample": {
        "language": "http",
        "code": "GET /users/123?fields=first_name,last_name,email"
      },
      "unlockLevel": 4,
      "tags": [
        "api",
        "performance",
        "optimization"
      ],
      "category": "processes"
    },
    {
      "id": "http0230-0000-0000-0000-000000000023",
      "word": "Throttling",
      "pronunciation": "THRAHT-ling",
      "partOfSpeech": "verb",
      "shortDefinition": "Limiting the number of times a function can be called over a certain period.",
      "simpleDefinition": "A mechanism that limits how often a function can execute over a specific time interval — ensuring a consistent execution rate. It prevents performance degradation from high-frequency events like scrolling or mouse movement by enforcing a maximum firing speed.",
      "longDefinition": "Unlike debouncing, throttling guarantees that the function will be called at most once per specified time interval. It is used to provide a consistent rate of execution.",
      "connector": "Think of throttling like a water-flow restrictor — no matter how much pressure there is, only a steady, manageable amount of water can flow through.",
      "techContext": "Essential for performance-heavy events that fire rapidly, such as 'scroll' or 'resize'.",
      "exampleSentence": "The scroll listener was causing major lag, so we throttled it to fire only once every 100ms.",
      "etymology": "from 'throttle' (to control the flow of fuel/power).",
      "codeExample": {
        "language": "js",
        "code": "function throttle(func, limit) {\n  let inThrottle;\n  return (...args) => {\n    if (!inThrottle) {\n      func(...args);\n      inThrottle = true;\n      setTimeout(() => inThrottle = false, limit);\n    }\n  };\n}"
      },
      "unlockLevel": 4,
      "tags": [
        "performance",
        "javascript",
        "optimization"
      ],
      "category": "processes"
    },
    {
      "id": "http0240-0000-0000-0000-000000000024",
      "word": "Content Negotiation",
      "pronunciation": "KON-tent nuh-goh-shee-AY-shun",
      "partOfSpeech": "noun",
      "shortDefinition": "Mechanism for serving different resource representations based on client preferences.",
      "simpleDefinition": "Process where clients and servers agree on the optimal data format for a response — using headers like 'Accept'. This allows a single URL to serve diverse formats like JSON, XML, or HTML.",
      "longDefinition": "Content negotiation allows a server to provide multiple representations of the same resource at a single URI. By examining headers such as 'Accept' and 'Accept-Language', the server can determine whether the client prefers JSON, XML, or a specific human language. This ensures that the API remains flexible and can support a wide range of consumers, from legacy systems requiring XML to modern web applications optimized for JSON, all while maintaining a consistent and clean interface for the developer.",
      "connector": "Think of content negotiation like a multilingual menu — you ask for the language you prefer and get the exact same information back.",
      "techContext": "Our API uses content negotiation to serve both standard JSON and a specialized binary format for our high-speed internal clients.",
      "exampleSentence": "The server returned a 406 Not Acceptable because the client requested an image format that we don't support.",
      "etymology": "from 'content' + Latin negotiatio ('business, traffic').",
      "codeExample": {
        "language": "http",
        "code": "GET /reports/1 HTTP/1.1\nAccept: application/pdf"
      },
      "unlockLevel": 4,
      "tags": [
        "api",
        "rest",
        "advanced"
      ],
      "category": "processes"
    },
    {
      "id": "http0250-0000-0000-0000-000000000025",
      "word": "HATEOAS",
      "pronunciation": "HAY-tee-ohs",
      "partOfSpeech": "acronym",
      "shortDefinition": "REST constraint using hypermedia links to drive application state and navigation.",
      "simpleDefinition": "REST constraint where API responses include links to related resources — allowing clients to discover and navigate available actions dynamically. This decouples the client from hardcoded URLs by using hypermedia to drive the application's current state.",
      "longDefinition": "HATEOAS stands for Hypermedia as the Engine of Application State, representing the highest maturity level of RESTful design. It allows a client to interact with the API through links provided dynamically by the server. Instead of hardcoding URLs, the client follows relationships defined in the response. This pattern decouples client logic from URI structures, allowing backend developers to evolve the API's path hierarchy without breaking older clients that navigate via hypermedia.",
      "connector": "Think of HATEOAS like a choose-your-own-adventure book — each page tells you which pages you can turn to next based on your current location.",
      "techContext": "By following HATEOAS, our API response for a 'User' includes links to their 'Orders', 'Settings', and 'Profile' resources.",
      "exampleSentence": "HATEOAS makes an API truly self-discoverable by providing the client with all the possible next steps in the current context.",
      "etymology": "Acronym for Hypermedia as the Engine of Application State.",
      "codeExample": {
        "language": "json",
        "code": "{\n  \"id\": 123,\n  \"links\": [\n    { \"rel\": \"self\", \"href\": \"/users/123\" },\n    { \"rel\": \"orders\", \"href\": \"/users/123/orders\" }\n  ]\n}"
      },
      "unlockLevel": 5,
      "tags": [
        "api",
        "rest",
        "architecture"
      ],
      "category": "concepts"
    }
  ]
}
