{
  "stack": "basic-web",
  "words": [
    {
      "id": "bw001000-0000-0000-0000-000000000001",
      "word": "HTTP",
      "pronunciation": "H-T-T-P",
      "partOfSpeech": "noun",
      "shortDefinition": "The protocol browsers and servers use to exchange data on the web.",
      "simpleDefinition": "The language browsers and servers use to talk to each other — a postal system with rules for how to ask for things and how to reply.",
      "longDefinition": "HyperText Transfer Protocol defines how clients request resources and servers respond. It is stateless and text-based. HTTP/1.1 introduced persistent connections; HTTP/2 added multiplexing; HTTP/3 runs over QUIC for lower latency.",
      "techContext": "Every web API call is HTTP. Understanding methods, status codes, headers, and keep-alive is essential for debugging network issues.",
      "exampleSentence": "The API was returning HTTP 200 even on errors, making client-side error handling nearly impossible.",
      "etymology": "coined by Tim Berners-Lee at CERN in 1989 alongside HTML and the first web server.",
      "connector": "Think of HTTP like ordering at a restaurant — you make a specific request (GET the menu) and the kitchen sends back a response.",
      "codeExample": {
        "language": "http",
        "code": "GET /users/42 HTTP/1.1\nHost: api.example.com\nAuthorization: Bearer <token>\n\n→ HTTP/1.1 200 OK\n→ Content-Type: application/json"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "networking",
        "protocol"
      ]
    },
    {
      "id": "bw002000-0000-0000-0000-000000000002",
      "word": "REST",
      "pronunciation": "REST",
      "partOfSpeech": "noun",
      "shortDefinition": "An architectural style for APIs that uses HTTP methods and resource-based URLs.",
      "simpleDefinition": "A set of rules for building web APIs where every piece of data has its own address and you interact with it using standard actions: get, create, update, delete.",
      "longDefinition": "Representational State Transfer defines six constraints — statelessness, uniform interface, client-server separation, cacheability, layered system, and optional code on demand. A RESTful API maps CRUD operations to HTTP methods (GET, POST, PUT, DELETE) on noun-based endpoints.",
      "techContext": "REST is the dominant API style for web services. Its stateless nature makes it horizontally scalable by default.",
      "exampleSentence": "We designed the API following REST conventions so each resource had a predictable URL and the correct HTTP method.",
      "etymology": "coined by Roy Fielding in his 2000 PhD dissertation at UC Irvine.",
      "connector": "Think of REST like a filing cabinet — each folder (resource) has a fixed location, and you open, add, change, or remove files using the same operations on any folder.",
      "codeExample": {
        "language": "http",
        "code": "GET    /articles        # list\nPOST   /articles        # create\nGET    /articles/42     # read\nPUT    /articles/42     # update\nDELETE /articles/42     # delete"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "api",
        "architecture"
      ]
    },
    {
      "id": "bw003000-0000-0000-0000-000000000003",
      "word": "API",
      "pronunciation": "A-P-I",
      "partOfSpeech": "noun",
      "shortDefinition": "A defined contract that lets one program communicate with another.",
      "simpleDefinition": "A contract that says 'here's what you can ask me to do, and here's what I'll give back' — it lets two programs talk without knowing how each other works inside.",
      "longDefinition": "An Application Programming Interface specifies how software components interact: what operations are available, what inputs they accept, and what outputs they return. Web APIs use HTTP; library APIs expose functions; OS APIs expose system calls.",
      "techContext": "APIs are everywhere — you consume them (third-party services) and produce them (your backend). Good API design is about clarity and stability.",
      "exampleSentence": "We versioned the API so breaking changes in v2 didn't affect clients still using v1.",
      "etymology": "first appeared in IBM documentation in the 1960s; popularised in web development in the 2000s.",
      "connector": "Think of an API like a TV remote — you press a button (call the API) and the TV does something (returns data), without needing to know how the TV works inside.",
      "codeExample": {
        "language": "ts",
        "code": "// Consuming a REST API\nconst user = await fetch('/api/users/42')\n  .then(res => res.json());\n\nconsole.log(user.name);"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "integration",
        "contracts"
      ]
    },
    {
      "id": "bw004000-0000-0000-0000-000000000004",
      "word": "endpoint",
      "pronunciation": "END-point",
      "partOfSpeech": "noun",
      "shortDefinition": "A specific URL at which an API is accessible and responds to requests.",
      "simpleDefinition": "A specific address you send requests to — like a phone extension inside a company, where different extensions handle different tasks.",
      "longDefinition": "An endpoint is the combination of an HTTP method and a URL path that maps to a handler function. Well-designed endpoints use plural nouns (/users), avoid verbs (/getUser), and nest resources logically (/users/42/orders).",
      "techContext": "Endpoint design decisions (naming, versioning, nesting depth) are hard to change once clients depend on them.",
      "exampleSentence": "The mobile team was hitting the wrong endpoint because the staging URL had not been updated in the config.",
      "etymology": "compound of end + point; adopted from networking (cable termination) to mean an API access point.",
      "connector": "Think of an endpoint like a specific counter at the DMV — each counter (URL + method) handles one task, and you have to go to the right one.",
      "codeExample": {
        "language": "ts",
        "code": "// Express.js endpoint\napp.get('/users/:id', async (req, res) => {\n  const user = await db.users.find(req.params.id);\n  res.json(user);\n});"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "api",
        "routing"
      ]
    },
    {
      "id": "bw006000-0000-0000-0000-000000000006",
      "word": "CORS",
      "pronunciation": "KORZ",
      "partOfSpeech": "noun",
      "shortDefinition": "A browser security mechanism that controls which origins can make requests to a different domain.",
      "simpleDefinition": "A browser safety rule that stops websites from secretly making requests to other sites on your behalf — unless the target site explicitly says it's OK.",
      "longDefinition": "Cross-Origin Resource Sharing lets servers declare which external domains may access their resources via HTTP headers. Browsers enforce the same-origin policy by default; CORS headers opt out of that restriction. Preflight OPTIONS requests are sent for non-simple requests.",
      "techContext": "CORS errors are one of the most common frustrations in web development. The fix is always server-side — add the right Access-Control-Allow-Origin header.",
      "exampleSentence": "The frontend was blocked by a CORS error because the backend hadn't whitelisted the staging domain.",
      "etymology": "acronym coined by W3C in the 2009 CORS specification draft.",
      "connector": "Think of CORS like a bouncer at a club — your browser asks 'is this request on the guest list?' and only lets it through if the server says yes.",
      "codeExample": {
        "language": "ts",
        "code": "// Express — allow specific origin\napp.use((req, res, next) => {\n  res.header('Access-Control-Allow-Origin',\n             'https://app.example.com');\n  next();\n});"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "security",
        "browser"
      ]
    },
    {
      "id": "bw007000-0000-0000-0000-000000000007",
      "word": "cookie",
      "pronunciation": "KUK-ee",
      "partOfSpeech": "noun",
      "shortDefinition": "A small piece of data a server sends to the browser to persist state across requests.",
      "simpleDefinition": "A small note the server sticks in your browser so it can remember who you are on your next visit — like a stamp on your hand at a venue.",
      "longDefinition": "Cookies are key-value pairs stored in the browser and automatically sent with every matching HTTP request. They carry session IDs, preferences, and tracking identifiers. Important attributes: HttpOnly (no JS access), Secure (HTTPS only), SameSite (CSRF protection), and Max-Age (expiry).",
      "techContext": "Session cookies expire when the browser closes; persistent cookies last until Max-Age. HttpOnly and Secure flags are security essentials.",
      "exampleSentence": "We set the session cookie with HttpOnly and Secure flags to prevent XSS from stealing it.",
      "etymology": "named by Lou Montulli at Netscape (1994) after the computing term 'magic cookie' — a token passed between programs.",
      "connector": "Think of a cookie like a cloakroom ticket — you get one when you check in, and you hand it back on every request so the server knows it's still you.",
      "codeExample": {
        "language": "ts",
        "code": "res.cookie('sessionId', token, {\n  httpOnly: true,   // no JS access\n  secure: true,     // HTTPS only\n  sameSite: 'strict', // CSRF guard\n  maxAge: 86400000  // 1 day\n});"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "auth",
        "browser"
      ]
    },
    {
      "id": "bw008000-0000-0000-0000-000000000008",
      "word": "webhook",
      "pronunciation": "WEB-hook",
      "partOfSpeech": "noun",
      "shortDefinition": "An HTTP callback that lets one service notify another when an event occurs.",
      "simpleDefinition": "Instead of your app repeatedly asking 'did anything happen?', the other service calls you the moment something does — like a text alert instead of refreshing a tracking page.",
      "longDefinition": "Webhooks invert the polling model — instead of your app asking 'did anything happen?', the source service pushes an HTTP POST to your URL when an event fires. They power Stripe payment confirmations, GitHub CI triggers, and Slack notifications.",
      "techContext": "Always verify webhook signatures to prevent spoofed events. Return 200 quickly and process asynchronously to avoid delivery timeouts.",
      "exampleSentence": "We processed Stripe payments via webhook so our database updated the moment the charge succeeded, not when the user navigated back.",
      "etymology": "coined by Jeff Lindsay in 2007 in a blog post proposing 'web hooks' as a reverse HTTP pattern.",
      "connector": "Think of a webhook like a doorbell — instead of checking the door every minute, the system pings you the moment someone arrives.",
      "codeExample": {
        "language": "ts",
        "code": "app.post('/webhooks/stripe', (req, res) => {\n  const sig = req.headers['stripe-signature'];\n  const event = stripe.webhooks.constructEvent(\n    req.body, sig, process.env.WEBHOOK_SECRET);\n  queue.push(event); // process async\n  res.sendStatus(200); // ack fast\n});"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "integration",
        "events"
      ]
    },
    {
      "id": "bw009000-0000-0000-0000-000000000009",
      "word": "middleware",
      "pronunciation": "MID-ul-wair",
      "partOfSpeech": "noun",
      "shortDefinition": "A function that sits in the request-response pipeline and can inspect, modify, or terminate requests.",
      "simpleDefinition": "Code that runs between a request arriving and your actual handler — checking, logging, or transforming it before it gets to its destination.",
      "longDefinition": "Middleware chains functions that each receive the request, optionally transform it, then pass it to the next handler. Common uses: authentication, logging, rate limiting, compression, and CORS. The order of middleware matters — auth must run before protected routes.",
      "techContext": "Thinking of a web server as a middleware pipeline helps keep cross-cutting concerns (auth, logging) separate from business logic.",
      "exampleSentence": "We added a middleware that logged every request's duration so we could spot slow endpoints in production.",
      "etymology": "from mid- + software; originally described software gluing mainframes to applications (1960s), repurposed for web frameworks in the 2000s.",
      "connector": "Think of middleware like a conveyor belt at airport security — every bag (request) passes through the same checks before it reaches its destination.",
      "codeExample": {
        "language": "ts",
        "code": "// Logger middleware\nconst logger = (req, res, next) => {\n  const start = Date.now();\n  res.on('finish', () =>\n    console.log(`${req.method} ${req.path} ${Date.now()-start}ms`));\n  next(); // pass to next handler\n};\napp.use(logger);"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "backend",
        "architecture"
      ]
    },
    {
      "id": "bw010000-0000-0000-0000-000000000010",
      "word": "JSON",
      "pronunciation": "JAY-sawn",
      "partOfSpeech": "noun",
      "shortDefinition": "A lightweight text format for representing structured data as key-value pairs and arrays.",
      "simpleDefinition": "A way to write structured data as text using curly braces and key-value pairs — so any programming language can read and write it.",
      "longDefinition": "JavaScript Object Notation is language-agnostic despite its name. It supports strings, numbers, booleans, null, objects, and arrays. JSON replaced XML as the dominant web data format due to its readability and native JavaScript compatibility.",
      "techContext": "Used for REST API payloads, config files, and data storage. JSON has no support for comments, dates, or binary — common gotchas.",
      "exampleSentence": "The API returned a JSON payload with a nested user object, but the mobile client expected a flat structure.",
      "etymology": "introduced and popularised by Douglas Crockford around 2001, standardised as ECMA-404 in 2013.",
      "connector": "Think of JSON like a standardised form — everyone fills in the same labelled boxes, so any system can read it regardless of the language it speaks.",
      "codeExample": {
        "language": "json",
        "code": "{\n  \"id\": 42,\n  \"name\": \"Ada Lovelace\",\n  \"roles\": [\"admin\", \"user\"],\n  \"active\": true,\n  \"metadata\": null\n}"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "data-format",
        "api"
      ]
    },
    {
      "id": "bw005000-0000-0000-0000-000000000005",
      "word": "status code",
      "pronunciation": "STAY-tus KODE",
      "partOfSpeech": "noun",
      "shortDefinition": "A three-digit number in an HTTP response indicating whether the request succeeded or failed.",
      "simpleDefinition": "A three-digit number every server response includes that tells you instantly whether things went well (200), you made a mistake (400), or the server broke (500).",
      "longDefinition": "HTTP status codes are grouped by first digit: 2xx (success), 3xx (redirect), 4xx (client error), 5xx (server error). Returning the correct code is critical — returning 200 on errors causes clients to mishandle failures.",
      "techContext": "Common codes every developer must know: 200 OK, 201 Created, 400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 422 Unprocessable Entity, 429 Too Many Requests, 500 Internal Server Error.",
      "exampleSentence": "The client was retrying on 400 errors, which it should never do — 400 means the request itself is wrong.",
      "etymology": "standardised by IETF in RFC 2616 (1999), evolved through subsequent RFCs.",
      "connector": "Think of status codes like a traffic light — green (2xx) means go, yellow (3xx) means follow the redirect, red (4xx/5xx) means something went wrong.",
      "codeExample": {
        "language": "ts",
        "code": "// Correct status codes\nres.status(201).json({ id: newUser.id }); // created\nres.status(404).json({ error: 'Not found' });\nres.status(422).json({ error: 'Invalid email' });"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "http",
        "api"
      ]
    },
    {
      "id": "bw011000-0000-0000-0000-000000000011",
      "word": "URL",
      "pronunciation": "U-R-L",
      "partOfSpeech": "acronym",
      "shortDefinition": "Uniform Resource Locator - the address of a resource on the web.",
      "simpleDefinition": "The full address of a resource on the internet — it tells your browser the protocol, the website, and exactly which page or piece of data to fetch.",
      "longDefinition": "A URL specifies the protocol, domain, port, path, query string, and fragment needed to locate a resource. Format: protocol://domain:port/path?query#fragment. URLs are a subset of URIs (Uniform Resource Identifiers).",
      "techContext": "URL encoding transforms special characters into %XX format. Understanding URL structure is essential for routing, security, and debugging.",
      "exampleSentence": "The redirect failed because we forgot to URL-encode the query parameter, breaking the URL when it contained an ampersand.",
      "etymology": "coined by Tim Berners-Lee in 1994; formalized in RFC 1738.",
      "connector": "Think of a URL like a postal address — the protocol is the delivery method, the domain is the building, and the path is the specific apartment number.",
      "codeExample": {
        "language": "bash",
        "code": "https://api.example.com:443/users/42?include=orders&format=json#profile\n\nprotocol: https\ndomain:   api.example.com\nport:     443\npath:     /users/42\nquery:    include=orders&format=json\nfragment: profile"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "fundamentals",
        "addressing"
      ]
    },
    {
      "id": "bw012000-0000-0000-0000-000000000012",
      "word": "query parameter",
      "pronunciation": "KWEER-ee puh-RAM-ih-ter",
      "partOfSpeech": "noun",
      "shortDefinition": "A key-value pair appended to a URL after a question mark to pass data to the server.",
      "simpleDefinition": "Extra options you attach to a URL after a question mark — like search filters you hand to the server alongside your request.",
      "longDefinition": "Query parameters provide optional inputs to an endpoint without changing the path. Multiple parameters are separated by &. They're visible in URLs, logged by proxies, and should never contain sensitive data. Used for filtering, pagination, and search.",
      "techContext": "Query params are idempotent (safe for GET requests). For non-idempotent operations or sensitive data, use the request body.",
      "exampleSentence": "We paginated the results using query parameters like ?page=2&limit=20 so clients could bookmark specific pages.",
      "etymology": "query from Latin quaerere (\"to seek\"); parameter from Greek para- (\"beside\") + metron (\"measure\").",
      "connector": "Think of query parameters like pizza toppings — the base pizza (URL) stays the same, but you append extras to customise the order.",
      "codeExample": {
        "language": "http",
        "code": "GET /api/users?role=admin&active=true&page=2 HTTP/1.1\n\n# Query params:\n# role=admin\n# active=true\n# page=2"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "api",
        "url"
      ]
    },
    {
      "id": "bw013000-0000-0000-0000-000000000013",
      "word": "header",
      "pronunciation": "HED-er",
      "partOfSpeech": "noun",
      "shortDefinition": "Metadata sent with an HTTP request or response to provide context about the message.",
      "simpleDefinition": "Extra information attached to the top of an HTTP message — the sender's instructions about what's inside and how to handle it, before the actual content.",
      "longDefinition": "Headers carry information like content type, authentication tokens, caching directives, and accepted encodings. Request headers (e.g., Authorization, User-Agent) come from the client; response headers (e.g., Content-Type, Set-Cookie) come from the server.",
      "techContext": "Essential headers: Content-Type (payload format), Authorization (credentials), Cache-Control (caching rules), Accept (preferred format).",
      "exampleSentence": "The API rejected the request because we sent a JSON body but forgot the Content-Type: application/json header.",
      "etymology": "from head + -er; in HTTP, headers come at the 'head' of the message before the body.",
      "connector": "Think of HTTP headers like the cover sheet on a fax — it describes the contents, who sent it, and how to handle it before you reach the actual document.",
      "codeExample": {
        "language": "http",
        "code": "POST /api/users HTTP/1.1\nHost: api.example.com\nContent-Type: application/json\nAuthorization: Bearer eyJhbGc...\n\n{\"name\": \"Ada\"}"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "http",
        "metadata"
      ]
    },
    {
      "id": "bw014000-0000-0000-0000-000000000014",
      "word": "authentication",
      "pronunciation": "aw-then-tih-kay-SHUN",
      "partOfSpeech": "noun",
      "shortDefinition": "Verifying the identity of a user or system making a request.",
      "simpleDefinition": "Proving who you are — like showing your ID at the door. The system checks your credentials before letting you in.",
      "longDefinition": "Authentication answers 'who are you?' Common mechanisms: username/password, API keys, JWTs, OAuth tokens, biometrics. Authentication precedes authorization — you must prove your identity before the system checks your permissions.",
      "techContext": "Always use HTTPS for authentication to prevent credential interception. Multi-factor authentication adds a second proof beyond passwords.",
      "exampleSentence": "The login flow uses password authentication first, then sends a JWT for subsequent API requests.",
      "etymology": "from Greek authentikos (\"genuine\") from authentes (\"one acting on one's own authority\").",
      "connector": "Think of authentication like unlocking your phone — you prove it's you (face, PIN, password) before the system lets you do anything.",
      "codeExample": {
        "language": "http",
        "code": "# Session-based auth:\nPOST /login HTTP/1.1\n{\"email\": \"ada@example.com\", \"password\": \"...\"}\n\n→ Set-Cookie: session=abc123; HttpOnly; Secure"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "security",
        "auth"
      ]
    },
    {
      "id": "bw015000-0000-0000-0000-000000000015",
      "word": "authorization",
      "pronunciation": "aw-thor-ih-ZAY-shun",
      "partOfSpeech": "noun",
      "shortDefinition": "Determining what actions an authenticated user is permitted to perform.",
      "simpleDefinition": "Once the system knows who you are, authorization decides what you're allowed to do — like a badge that gets you in the building but not the server room.",
      "longDefinition": "Authorization answers 'what can you do?' It happens after authentication. Common models: role-based (admin, user), permission-based (can_edit, can_delete), or attribute-based. Returns HTTP 403 Forbidden if denied.",
      "techContext": "Confused deputy problem occurs when authorization checks are missing or inconsistent across endpoints.",
      "exampleSentence": "Even though the user authenticated successfully, authorization failed because they lacked the admin role required to delete accounts.",
      "etymology": "from Latin auctor (\"originator, promoter\") + -ize + -ation — granting authority.",
      "connector": "Think of authorization like a permission slip — it's not about who you are, it's about which specific things you're allowed to do.",
      "codeExample": {
        "language": "ts",
        "code": "// Authorization middleware\nfunction requireAdmin(req, res, next) {\n  if (req.user.role !== 'admin') {\n    return res.status(403).json({ error: 'Forbidden' });\n  }\n  next();\n}"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "security",
        "auth"
      ]
    },
    {
      "id": "bw016000-0000-0000-0000-000000000016",
      "word": "session",
      "pronunciation": "SESH-un",
      "partOfSpeech": "noun",
      "shortDefinition": "A period of interaction between a user and a server, tracked via a session identifier.",
      "simpleDefinition": "A way for a server to remember you across multiple requests — since HTTP is stateless by design, sessions store who you are so you don't log in on every click.",
      "longDefinition": "Sessions persist user state across multiple HTTP requests. The server stores session data (user ID, cart items) and sends a session ID to the client as a cookie. The client includes this ID with subsequent requests. Sessions expire after timeout or logout.",
      "techContext": "Session storage can be in-memory, database, or Redis. Horizontal scaling requires sticky sessions or shared session storage.",
      "exampleSentence": "The user's cart was empty after load balancing sent them to a different server because we weren't using shared session storage.",
      "etymology": "from Latin sessio (\"a sitting\") from sedere (\"to sit\") — the time spent 'sitting' with the application.",
      "connector": "Think of a session like a tab at a bar — the bartender remembers it's you for the whole evening without you re-introducing yourself for every drink.",
      "codeExample": {
        "language": "ts",
        "code": "// Express session:\napp.use(session({\n  secret: 'key',\n  resave: false,\n  saveUninitialized: false,\n  cookie: { maxAge: 86400000 }  // 1 day\n}));\n\nreq.session.userId = 42;  // store data"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "auth",
        "state"
      ]
    },
    {
      "id": "bw017000-0000-0000-0000-000000000017",
      "word": "JWT",
      "pronunciation": "J-W-T",
      "partOfSpeech": "acronym",
      "shortDefinition": "JSON Web Token - a self-contained token that carries user claims and is cryptographically signed.",
      "simpleDefinition": "A self-contained token that carries your identity and permissions, signed so no one can tamper with it — the server verifies it without querying a database.",
      "longDefinition": "JWTs encode user identity and permissions in a JSON payload, then sign it with a secret or private key. The client stores the JWT and sends it with each request. The server verifies the signature without querying a database. JWTs are stateless but can't be revoked without additional infrastructure.",
      "techContext": "Common for API authentication and SSO. Three parts: header.payload.signature. Never store sensitive data in the payload — it's base64, not encrypted.",
      "exampleSentence": "We switched from sessions to JWTs so our stateless API could scale horizontally without session affinity.",
      "etymology": "defined in RFC 7519 (2015); widely adopted for stateless authentication.",
      "connector": "Think of a JWT like a signed concert wristband — it shows your access level, anyone can verify it's genuine, and you don't need to check back with the box office for every song.",
      "codeExample": {
        "language": "bash",
        "code": "# JWT structure:\neyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.      # header\neyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkFkYSJ9.  # payload\nSflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c      # signature\n\n# Client sends:\nAuthorization: Bearer <jwt>"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "auth",
        "security"
      ]
    },
    {
      "id": "bw018000-0000-0000-0000-000000000018",
      "word": "HTTPS",
      "pronunciation": "H-T-T-P-S",
      "partOfSpeech": "acronym",
      "shortDefinition": "HTTP Secure - HTTP encrypted with TLS to protect data in transit.",
      "simpleDefinition": "HTTP with a lock on it — everything you send is scrambled so anyone listening on the network can't read it. Essential for passwords and personal data.",
      "longDefinition": "HTTPS wraps HTTP traffic in TLS encryption, preventing eavesdropping and tampering. Requires an SSL/TLS certificate. Browsers show a padlock icon and warn users when connections aren't secure. Essential for transmitting passwords, tokens, or personal data.",
      "techContext": "Always use HTTPS in production. Modern browsers block features like geolocation and camera access on HTTP sites.",
      "exampleSentence": "We enforce HTTPS by redirecting all HTTP requests to port 443 and setting the HSTS header to prevent downgrade attacks.",
      "etymology": "HTTPS adopted in 1994 by Netscape; TLS (Transport Layer Security) replaced SSL in 1999.",
      "connector": "Think of HTTPS vs HTTP like a sealed envelope vs a postcard — HTTP is the postcard (anyone can read it), HTTPS seals it so only the recipient can.",
      "codeExample": {
        "language": "nginx",
        "code": "# Nginx redirect HTTP → HTTPS:\nserver {\n  listen 80;\n  return 301 https://$host$request_uri;\n}\n\nserver {\n  listen 443 ssl;\n  ssl_certificate     /path/to/cert.pem;\n  ssl_certificate_key /path/to/key.pem;\n}"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "security",
        "encryption"
      ]
    },
    {
      "id": "bw019000-0000-0000-0000-000000000019",
      "word": "WebSocket",
      "pronunciation": "WEB-sok-et",
      "partOfSpeech": "noun",
      "shortDefinition": "A protocol for persistent, bidirectional communication between client and server over a single TCP connection.",
      "simpleDefinition": "A permanent two-way connection between browser and server — unlike HTTP where you ask and wait, either side can send a message any time.",
      "longDefinition": "WebSockets upgrade an HTTP connection to a long-lived socket, enabling real-time push notifications without polling. After the handshake, both client and server can send messages freely. Used for chat, live updates, and multiplayer games.",
      "techContext": "Contrast with HTTP polling (inefficient) and Server-Sent Events (one-way). WebSockets require different scaling strategies than stateless HTTP.",
      "exampleSentence": "We used WebSockets for the trading dashboard so price updates appeared instantly without the client polling every second.",
      "etymology": "standardized as RFC 6455 in 2011; 'socket' from BSD sockets API (1983).",
      "connector": "Think of WebSocket like a phone call — once connected, both sides can speak whenever they want, instead of HTTP's text-message model where you send and wait for a reply.",
      "codeExample": {
        "language": "js",
        "code": "// Client:\nconst ws = new WebSocket('wss://example.com/socket');\nws.onmessage = (event) => {\n  console.log('Received:', event.data);\n};\nws.send('Hello server!');"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "real-time",
        "protocol"
      ]
    },
    {
      "id": "bw020000-0000-0000-0000-000000000020",
      "word": "GraphQL",
      "pronunciation": "GRAF-Q-L",
      "partOfSpeech": "noun",
      "shortDefinition": "A query language for APIs that lets clients request exactly the data they need.",
      "simpleDefinition": "A way to query a server where you specify exactly which fields you want back — no more, no less — instead of getting a fixed response shape.",
      "longDefinition": "GraphQL provides a strongly typed schema defining available queries and mutations. Clients specify which fields to retrieve, eliminating over-fetching and under-fetching. A single endpoint handles all queries. Developed by Facebook; open-sourced in 2015.",
      "techContext": "Contrasts with REST where each endpoint returns a fixed structure. GraphQL introduces complexity (caching, N+1 queries) but offers flexibility.",
      "exampleSentence": "We adopted GraphQL so the mobile app could fetch user, posts, and comments in a single request instead of hitting three REST endpoints.",
      "etymology": "Graph (data structure) + QL (Query Language); released by Facebook in 2015.",
      "connector": "Think of GraphQL like a custom sandwich order — instead of choosing from a fixed menu (REST endpoints), you specify exactly what you want layer by layer.",
      "codeExample": {
        "language": "graphql",
        "code": "# Query:\nquery {\n  user(id: \"42\") {\n    name\n    email\n    posts {\n      title\n      comments { author }\n    }\n  }\n}\n\n# Client requests only needed fields"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "api",
        "query-language"
      ]
    },
    {
      "id": "bw021000-0000-0000-0000-000000000021",
      "word": "AJAX",
      "pronunciation": "AY-jaks",
      "partOfSpeech": "acronym",
      "shortDefinition": "Asynchronous JavaScript and XML - a technique for updating web pages without full refresh.",
      "simpleDefinition": "A technique for updating part of a web page with fresh server data — without reloading the whole page.",
      "longDefinition": "AJAX uses JavaScript to send HTTP requests in the background and update parts of the page with the response. Despite the name, JSON has largely replaced XML. AJAX enabled the shift from server-rendered pages to dynamic single-page applications.",
      "techContext": "Modern equivalent: fetch() API. AJAX revolutionized web UX by enabling Gmail, Google Maps, and dynamic interfaces.",
      "exampleSentence": "We used AJAX to load search results as the user typed, providing instant feedback without navigating to a new page.",
      "etymology": "coined by Jesse James Garrett in 2005; acronym for Asynchronous JavaScript and XML.",
      "connector": "Think of AJAX like a waiter who refills your water glass quietly — small updates happen in the background without you having to restart the whole meal.",
      "codeExample": {
        "language": "js",
        "code": "// Modern AJAX with fetch:\nfetch('/api/search?q=query')\n  .then(res => res.json())\n  .then(results => {\n    document.querySelector('#results').innerHTML = renderResults(results);\n  });"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "javascript",
        "async"
      ]
    },
    {
      "id": "bw022000-0000-0000-0000-000000000022",
      "word": "SPA",
      "pronunciation": "S-P-A",
      "partOfSpeech": "acronym",
      "shortDefinition": "Single Page Application - a web app that loads once and dynamically updates content via JavaScript.",
      "simpleDefinition": "A web app that loads once and swaps content as you navigate — so it feels fast and native, like a phone app, instead of reloading the entire page each time.",
      "longDefinition": "SPAs load an initial HTML shell, then use JavaScript to render views and fetch data via APIs. Navigation happens client-side without full page reloads. Frameworks: React, Vue, Angular. Trade-offs: faster interactions, worse SEO (unless server-side rendered), larger initial bundle.",
      "techContext": "SPAs blur the line between web and native apps. Client-side routing, state management, and code splitting are essential patterns.",
      "exampleSentence": "Our SPA felt snappy because navigation was instant, but SEO suffered until we added server-side rendering.",
      "etymology": "term emerged around 2002-2005 as AJAX-driven apps became common; popularized by frameworks in 2010s.",
      "connector": "Think of an SPA like a digital magazine — the cover loads once and you flip through pages instantly, rather than returning to the newsstand for each new article.",
      "codeExample": {
        "language": "jsx",
        "code": "// React SPA:\nfunction App() {\n  return (\n    <Router>\n      <Route path=\"/\" component={Home} />\n      <Route path=\"/users/:id\" component={UserProfile} />\n    </Router>\n  );\n}\n// No full page reload on navigation"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "frontend",
        "architecture"
      ]
    },
    {
      "id": "bw023000-0000-0000-0000-000000000023",
      "word": "SSR",
      "pronunciation": "S-S-R",
      "partOfSpeech": "acronym",
      "shortDefinition": "Server-Side Rendering - generating HTML on the server for each request.",
      "simpleDefinition": "Building the full page on the server before sending it to the browser — so it appears instantly and search engines can read the content without running JavaScript.",
      "longDefinition": "SSR produces fully-formed HTML on the server, sending complete pages to the client. Good for SEO, fast first paint, and works without JavaScript. Contrasts with CSR (client-side rendering). Modern frameworks (Next.js, Nuxt) support both SSR and static generation.",
      "techContext": "SSR improves SEO and perceived performance but increases server load and complexity. Often combined with client-side hydration.",
      "exampleSentence": "We added SSR to our React app with Next.js so search engines could crawl product pages and improve rankings.",
      "etymology": "term emerged to distinguish traditional server rendering from client-side SPAs; formalized in modern frameworks 2010s.",
      "connector": "Think of SSR like a pre-made meal vs. cooking at the table — the kitchen does all the work upfront so the food arrives ready to eat.",
      "codeExample": {
        "language": "jsx",
        "code": "// Next.js SSR:\nexport async function getServerSideProps(context) {\n  const user = await fetchUser(context.params.id);\n  return { props: { user } };\n}\n\nfunction UserPage({ user }) {\n  return <div>{user.name}</div>;\n}\n// HTML generated on server per request"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "frontend",
        "rendering"
      ]
    },
    {
      "id": "bw024000-0000-0000-0000-000000000024",
      "word": "redirect",
      "pronunciation": "ree-dih-REKT",
      "partOfSpeech": "noun",
      "shortDefinition": "An HTTP response instructing the client to request a different URL.",
      "simpleDefinition": "A server's way of saying 'what you want isn't here — go ask over there' — the browser automatically follows the new address without the user noticing.",
      "longDefinition": "Redirects use 3xx status codes: 301 (permanent, cached by browsers), 302 (temporary, not cached), 303 (after POST → GET), 307 (temporary, preserve method). The Location header specifies the target URL. Used for URL changes, HTTPS enforcement, and post-login flows.",
      "techContext": "Search engines treat 301s as canonical URL changes. 302s preserve original URLs. Choose carefully based on intent.",
      "exampleSentence": "We used a 301 redirect from the old domain to the new one so search rankings transferred over.",
      "etymology": "re- (\"again\") + direct from Latin directus (\"straight\"); first appeared in HTTP/1.0 (1996).",
      "connector": "Think of a redirect like a mail forwarding address — the post office sends it along to wherever you actually live now.",
      "codeExample": {
        "language": "http",
        "code": "HTTP/1.1 301 Moved Permanently\nLocation: https://example.com/new-path\n\n# Client automatically requests new URL"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "http",
        "routing"
      ]
    },
    {
      "id": "bw025000-0000-0000-0000-000000000025",
      "word": "payload",
      "pronunciation": "PAY-lode",
      "partOfSpeech": "noun",
      "shortDefinition": "The actual data transmitted in an HTTP request or response body.",
      "simpleDefinition": "The actual content of an HTTP message — the data you're sending or receiving, as opposed to the metadata in the headers.",
      "longDefinition": "The payload is the message content, distinct from headers and metadata. In HTTP requests, it's the JSON, XML, or form data sent to the server. In responses, it's the data returned to the client. Size limits, encoding, and content type are key considerations.",
      "techContext": "Large payloads impact performance. Use pagination, compression (gzip), or streaming for big datasets. Content-Type header describes payload format.",
      "exampleSentence": "The request failed because the JSON payload exceeded the server's 10MB limit for POST bodies.",
      "etymology": "from pay + load; originally aeronautical (revenue-generating cargo); adopted in networking for actual data vs. overhead.",
      "connector": "Think of a payload like the package inside a shipping box — the headers are the label and postage, but the payload is what you actually ordered.",
      "codeExample": {
        "language": "http",
        "code": "POST /api/users HTTP/1.1\nContent-Type: application/json\nContent-Length: 52\n\n{\"name\": \"Ada Lovelace\", \"email\": \"ada@example.com\"}\n# ↑ This JSON is the payload"
      },
      "stack": "basic-web",
      "unlockLevel": 1,
      "tags": [
        "web",
        "http",
        "data"
      ]
    }
  ]
}
