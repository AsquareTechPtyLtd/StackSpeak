{
  "stack": "docker-intermediate",
  "words": [
    {
      "id": "dock0010-0000-0000-0000-000000000001",
      "word": "Container",
      "pronunciation": "kun-TAY-nur",
      "partOfSpeech": "noun",
      "shortDefinition": "Lightweight, standalone, executable software package containing code and dependencies for isolation.",
      "simpleDefinition": "Portable software unit bundling application code with its runtime, system tools, and libraries — ensuring consistent execution across environments. It provides isolated process space while sharing the host kernel for efficiency.",
      "longDefinition": "Containers leverage Linux kernel features like namespaces and control groups to create isolated environments for processes. Unlike virtual machines, they do not include a guest OS, making them significantly faster to start and more resource-efficient. This isolation ensures that the application behaves identically whether running on a developer's laptop, a test server, or a production cluster, effectively solving the 'it works on my machine' problem.",
      "connector": "Think of a container like a standardized shipping box — it protects the contents and fits perfectly onto any compatible ship, train, or truck.",
      "techContext": "The fundamental unit of modern DevOps, allowing developers to say 'it works on my machine' and know it will work in production.",
      "exampleSentence": "We spun up a new container for the worker process to handle the sudden spike in background tasks.",
      "codeExample": {
        "language": "bash",
        "code": "# Run an interactive container\ndocker run -it --name my-app-instance node:20-alpine sh"
      },
      "etymology": "from Latin continere ('to hold together').",
      "unlockLevel": 1,
      "tags": [
        "docker",
        "devops",
        "infrastructure"
      ],
      "category": "components"
    },
    {
      "id": "dock0020-0000-0000-0000-000000000002",
      "word": "EXPOSE",
      "pronunciation": "eks-POHZ",
      "partOfSpeech": "noun",
      "shortDefinition": "A Dockerfile directive that documents which network ports the container will listen on at runtime.",
      "simpleDefinition": "`EXPOSE 8080` in a Dockerfile is metadata — it tells humans (and `docker run -P`) which ports to bind. It does NOT actually publish the port.",
      "longDefinition": "EXPOSE is one of Docker's most-misunderstood directives. It declares intent — 'this container expects traffic on these ports' — but doesn't actually publish them. To publish, you need `docker run -p 8080:8080` (or `-P` to map all EXPOSEd ports to random host ports).",
      "techContext": "EXPOSE matters for orchestrators (Docker Compose, Swarm, Kubernetes) that auto-discover service ports, and for `docker inspect` introspection. Skip it and your container still works, but tooling has less context.",
      "exampleSentence": "EXPOSE 8080 in the Dockerfile, then publish at deploy time with `-p 80:8080` — keeps the image agnostic to the host port.",
      "etymology": "Standard English — to expose (make visible/accessible).",
      "connector": "Like a TypeScript interface declaration — documents the contract without enforcing the implementation.",
      "codeExample": {
        "language": "dockerfile",
        "code": "FROM node:20-alpine\nWORKDIR /app\nCOPY package.json ./\nRUN npm install --production\nCOPY . .\nEXPOSE 3000           # documentation only\nCMD [\"node\", \"server.js\"]\n\n# At runtime, actually publish the port:\n#   docker run -p 3000:3000 myapp"
      },
      "unlockLevel": 1,
      "tags": [
        "docker",
        "dockerfile"
      ],
      "category": "syntax"
    },
    {
      "id": "dock0030-0000-0000-0000-000000000003",
      "word": "Dockerfile",
      "pronunciation": "DAK-ur-fyl",
      "partOfSpeech": "noun",
      "shortDefinition": "Text-based script containing sequential commands to automate Docker image creation.",
      "simpleDefinition": "Automated build script defining the environment, dependencies, and configuration for an image — enabling reproducible infrastructure as code. It translates human-readable instructions into the binary layers that form a portable container image.",
      "longDefinition": "The Dockerfile serves as the primary mechanism for automating image construction. It starts with a base image and then executes a series of instructions such as RUN for commands, COPY for files, and ENV for variables. By version-controlling the Dockerfile alongside the source code, teams can guarantee that the build environment remains identical across the entire development lifecycle, facilitating reliable CI/CD pipelines and easier troubleshooting.",
      "connector": "Think of a Dockerfile like a recipe card — anyone with the same ingredients can follow the steps to produce the exact same dish.",
      "techContext": "The 'recipe' for your environment. It should be kept in version control alongside your code.",
      "exampleSentence": "I updated the Dockerfile to include the latest security patches for our Node.js runtime.",
      "codeExample": {
        "language": "dockerfile",
        "code": "FROM node:20-alpine\nWORKDIR /app\nCOPY package*.json ./\nRUN npm install --production\nCOPY . .\nEXPOSE 3000\nCMD [\"node\", \"index.js\"]"
      },
      "etymology": "from 'Docker' + 'file'.",
      "unlockLevel": 1,
      "tags": [
        "docker",
        "devops",
        "automation"
      ],
      "category": "components"
    },
    {
      "id": "dock0040-0000-0000-0000-000000000004",
      "word": "Port Mapping",
      "pronunciation": "PORT MAP-ing",
      "partOfSpeech": "noun",
      "shortDefinition": "Networking configuration linking host ports to isolated container network ports.",
      "simpleDefinition": "Bridge between the host's public network and a container's private virtual network — exposing specific services to the outside world. It allows external traffic to reach applications running inside the container's isolated network environment.",
      "longDefinition": "Because containers run in an isolated network namespace, their internal ports are not automatically accessible from the host machine or external network. Port mapping creates a rule in the host's network stack that forwards traffic from a host-side port to a container-side port. This allows you to run multiple containers using the same internal port, such as 80, by mapping them to different unique ports on the host.",
      "connector": "Think of port mapping like a building's switchboard — calls to specific extensions are routed to the correct private office inside.",
      "techContext": "We mapped host port 80 to container port 3000 so that we could access our Node app via 'localhost' in the browser.",
      "exampleSentence": "I checked the port mapping because I couldn't connect to the database container on the default port.",
      "codeExample": {
        "language": "bash",
        "code": "# Map host port 8080 to container port 80\ndocker run -p 8080:80 nginx:alpine"
      },
      "etymology": "from 'port' (endpoint) + 'mapping'.",
      "unlockLevel": 1,
      "tags": [
        "docker",
        "networking",
        "fundamentals"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0050-0000-0000-0000-000000000005",
      "word": ".dockerignore",
      "pronunciation": "DOT DOK-er-ig-nor",
      "partOfSpeech": "noun",
      "shortDefinition": "A file listing patterns to exclude from the build context sent to the Docker daemon.",
      "simpleDefinition": "Like `.gitignore` for Docker builds — keeps `node_modules`, `.git`, `dist/`, secrets out of the image and the build context.",
      "longDefinition": "Without `.dockerignore`, Docker tars up the entire build directory and sends it to the daemon — every time. That includes `node_modules` (slow) and `.env` files (a security incident waiting to happen). A 5-line dockerignore can drop build context from 1GB to 5MB and prevent accidental secret leaks.",
      "techContext": "Order matters less than coverage — be aggressive. Always exclude: `.git`, `node_modules`, `*.log`, `.env*`, `dist/`, `build/`. The trade-off cost is approximately zero; the benefits are massive.",
      "exampleSentence": "Add a `.dockerignore` — the build context dropped from 800MB to 12MB, and CI got 4× faster.",
      "etymology": "Modeled directly on `.gitignore` — same syntax, same purpose.",
      "connector": "Like `.gitignore` for Docker — same file format, same wildcard rules.",
      "codeExample": {
        "language": "gitignore",
        "code": "# .dockerignore\n.git\n.gitignore\nnode_modules\nnpm-debug.log\n.env*\ndist\nbuild\n*.md\n.DS_Store\n.vscode\n.idea\nDockerfile\ndocker-compose*.yml"
      },
      "unlockLevel": 1,
      "tags": [
        "docker",
        "build"
      ],
      "category": "configuration"
    },
    {
      "id": "dock0060-0000-0000-0000-000000000006",
      "word": "Docker Hub",
      "pronunciation": "DAK-ur HUB",
      "partOfSpeech": "noun",
      "shortDefinition": "Public cloud-based registry for sharing and discovering containerized software images.",
      "simpleDefinition": "The largest community-driven repository for Docker images — hosting official versions of databases, runtimes, and open-source tools. It serves as the default source for pulling images, enabling rapid development and standardized software distribution across the globe.",
      "longDefinition": "Docker Hub acts as the central nerve center for the Docker ecosystem, providing a platform where developers can publish and download container images. It includes features like automated builds, security scanning, and private repositories. Most developers start their projects by pulling an official image from Docker Hub, which has been vetted and optimized for containerized environments. This collaborative library significantly reduces the time required to set up complex development stacks.",
      "connector": "Think of Docker Hub like GitHub for container images — it’s the place where the world shares and versions their software.",
      "techContext": "I searched Docker Hub to find a pre-configured image for our CI testing environment.",
      "exampleSentence": "We use Docker Hub to pull our base images, but we host our proprietary code images in our own private registry.",
      "codeExample": {
        "language": "bash",
        "code": "# Search for official images on Docker Hub\ndocker search --filter \"is-official=true\" redis"
      },
      "etymology": "from 'Docker' + 'hub' (a center of activity).",
      "unlockLevel": 1,
      "tags": [
        "docker",
        "infrastructure",
        "community"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0070-0000-0000-0000-000000000007",
      "word": "Image Manifest",
      "pronunciation": "IM-ij MAN-i-fest",
      "partOfSpeech": "noun",
      "shortDefinition": "A JSON descriptor that lists an image's layers, config, and metadata — the OCI spec for what an image actually is.",
      "simpleDefinition": "A JSON file the registry serves that says 'this image = these layers + this config + these annotations.' Pull an image, the manifest is the first thing fetched.",
      "longDefinition": "Every container image is, at the registry level, a manifest pointing at content-addressed blobs (layers, config). The manifest schema is defined by the OCI Image Spec. Multi-arch images use a 'manifest list' (or 'image index') that points at per-architecture manifests — Docker picks the right one for your host.",
      "techContext": "`docker manifest inspect <image>` shows the JSON. Use it to verify multi-arch coverage, audit layer counts, or debug 'why is this image so big' before pulling layers.",
      "exampleSentence": "Inspect the image manifest — there's a stray amd64-only layer pulling in 200MB on arm64 runners.",
      "etymology": "From shipping — a manifest is the cargo list. OCI adopted the term for the equivalent in container images.",
      "connector": "Like a `package.json` — the small descriptor that ties together all the actual content (dependencies, in npm; layers, in Docker).",
      "codeExample": {
        "language": "json",
        "code": "{\n  \"schemaVersion\": 2,\n  \"mediaType\": \"application/vnd.oci.image.manifest.v1+json\",\n  \"config\": { \"mediaType\": \"...config+json\", \"digest\": \"sha256:abc...\", \"size\": 1234 },\n  \"layers\": [\n    { \"mediaType\": \"...tar+gzip\", \"digest\": \"sha256:111...\", \"size\": 32100000 },\n    { \"mediaType\": \"...tar+gzip\", \"digest\": \"sha256:222...\", \"size\":   450000 }\n  ]\n}"
      },
      "unlockLevel": 2,
      "tags": [
        "docker",
        "oci"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0080-0000-0000-0000-000000000008",
      "word": "Tmpfs Mount",
      "pronunciation": "TEMP-eff-ess MOWNT",
      "partOfSpeech": "noun",
      "shortDefinition": "A container mount type backed entirely by RAM — data is lost when the container stops.",
      "simpleDefinition": "A mount where the contents live in host memory, never touch disk. Fast, ephemeral, and gone the moment the container exits.",
      "longDefinition": "Tmpfs mounts are the right answer for transient sensitive data: secrets you don't want hitting disk, scratch space for hot computation, fast cache that should die with the container. They cost RAM, not disk — and on Linux, they're literally a tmpfs filesystem mounted into the container's mount namespace.",
      "techContext": "Linux only (tmpfs is a kernel feature). On Windows, use `--mount type=npipe` or a different approach. Set a size cap (`--tmpfs /scratch:size=512m`) — without one, a runaway process can OOM the host.",
      "exampleSentence": "Mount the secrets directory as tmpfs — they shouldn't survive a container restart, let alone hit the host disk.",
      "etymology": "tmpfs = temporary filesystem (kernel terminology), mount = filesystem mount.",
      "connector": "Like a memory-backed scratch directory — fast, ephemeral, no persistence.",
      "codeExample": {
        "language": "bash",
        "code": "# tmpfs flag (Linux only, simpler)\ndocker run --tmpfs /tmp:size=64m,mode=1777 myimg\n\n# --mount form (more flexible)\ndocker run \\\n  --mount type=tmpfs,destination=/secrets,tmpfs-size=8m \\\n  myimg\n\n# Verify\ndocker exec mycontainer mount | grep tmpfs"
      },
      "unlockLevel": 2,
      "tags": [
        "docker",
        "storage"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0090-0000-0000-0000-000000000009",
      "word": "Registry",
      "pronunciation": "REJ-ih-stree",
      "partOfSpeech": "noun",
      "shortDefinition": "Centralized service for storing, versioning, and distributing named Docker images.",
      "simpleDefinition": "Storage and content delivery system for managing Docker images — facilitating sharing across teams and deployment pipelines. Registries serve as the authoritative source for containers, allowing developers to push new versions and servers to pull them.",
      "longDefinition": "A Docker registry is a system that allows you to store and distribute images. It organizes images into repositories, which can contain multiple tagged versions. Public registries like Docker Hub host official software images, while private registries are used by organizations to secure their proprietary code. Integration with CI/CD systems allows for automated image publishing, ensuring that the latest validated build is always available for deployment.",
      "connector": "Think of a registry like an app store for containers — it’s where you publish your work so others can download it.",
      "techContext": "Our CI/CD pipeline automatically builds a new image and pushes it to our private Azure Container Registry after every successful merge.",
      "exampleSentence": "The deployment failed because the production server didn't have the correct credentials to pull the image from our private registry.",
      "codeExample": {
        "language": "bash",
        "code": "# Log in to a registry and push an image\ndocker login myregistry.com\ndocker push myregistry.com/my-app:v1.2"
      },
      "etymology": "from Latin regestrum ('list, catalog').",
      "unlockLevel": 2,
      "tags": [
        "docker",
        "infrastructure",
        "cloud"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0100-0000-0000-0000-000000000010",
      "word": "Docker Compose",
      "pronunciation": "DAK-ur kum-POHZ",
      "partOfSpeech": "noun",
      "shortDefinition": "Orchestration tool for defining and running multi-container applications via YAML.",
      "simpleDefinition": "YAML-driven tool for managing complex, multi-container environments as a single stack — automating network and volume configuration. It enables developers to launch entire application suites, including databases and caches, with a single standardized command.",
      "longDefinition": "Docker Compose is essential for managing applications that consist of multiple interdependent services. By defining the entire stack in a docker-compose.yml file, you can specify environment variables, port mappings, and service dependencies. This eliminates the need for complex, error-prone shell scripts to start your infrastructure. It is particularly powerful for local development and testing, ensuring that everyone on the team is running the exact same stack of services.",
      "connector": "Think of Docker Compose like a conductor's score — it tells every instrument what to play so they all sound good together.",
      "techContext": "The standard way to manage a local development environment consisting of a web app, a database, and a cache.",
      "exampleSentence": "I added a Redis service to our docker-compose.yml so we can test the new caching logic locally.",
      "codeExample": {
        "language": "yaml",
        "code": "version: '3.8'\nservices:\n  api:\n    build: .\n    ports: [\"8080:8080\"]\n  db:\n    image: postgres:15-alpine"
      },
      "etymology": "from 'Docker' + 'compose' (to put together).",
      "unlockLevel": 2,
      "tags": [
        "docker",
        "orchestration",
        "tools"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0110-0000-0000-0000-000000000011",
      "word": "Alpine Linux",
      "pronunciation": "AL-pyn LIN-uks",
      "partOfSpeech": "noun",
      "shortDefinition": "Security-focused, lightweight Linux distribution commonly used for minimal Docker images.",
      "simpleDefinition": "Minimalist Linux distribution built on musl libc and BusyBox — weighing only 5MB as a base image. It is favored by Docker users for its tiny footprint, rapid pull times, and reduced surface area for security vulnerabilities.",
      "longDefinition": "Alpine Linux is designed with security and resource efficiency in mind. Its small size makes it perfect for containers, where minimizing image volume is crucial for fast scaling and low storage costs. However, because it uses musl libc instead of the more common glibc found in Ubuntu or Debian, some binaries may require additional configuration or recompilation to run correctly, necessitating careful testing during the migration process.",
      "connector": "Think of Alpine Linux like a tiny studio apartment — it’s stripped of all non-essentials to maximize efficiency and save space.",
      "techContext": "Most of our microservices use the alpine tag to keep our container registry usage low and deployments fast.",
      "exampleSentence": "Using the Node-Alpine image helped us cut our cold-start times in half because the image layer downloads so quickly.",
      "codeExample": {
        "language": "dockerfile",
        "code": "# Use Alpine for a tiny Python image\nFROM python:3.11-alpine\nRUN apk add --no-cache gcc musl-dev"
      },
      "etymology": "Named after the Alps mountains, implying something small and clean.",
      "unlockLevel": 2,
      "tags": [
        "docker",
        "linux",
        "optimization"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0120-0000-0000-0000-000000000012",
      "word": "OCI Image",
      "pronunciation": "OH-see-eye IM-ij",
      "partOfSpeech": "noun",
      "shortDefinition": "A container image conforming to the Open Container Initiative spec — the vendor-neutral standard for container image format.",
      "simpleDefinition": "A standardized image format any compliant runtime (Docker, containerd, podman, CRI-O) can pull and run — no vendor lock-in.",
      "longDefinition": "OCI was formed in 2015 to standardize container images and runtimes. An OCI image is the same JSON-manifest + content-addressed-layers structure Docker pioneered, but governed by a neutral spec. Every modern container tool reads and writes OCI by default — including Docker since v23.",
      "techContext": "Tools like `buildah`, `skopeo`, and `crane` operate directly on OCI images without needing a Docker daemon. Distroless and chainguard images are OCI-spec-compliant; they don't need any Docker-specific behavior to run.",
      "exampleSentence": "Build the images with buildah and push to GHCR — fully OCI-compliant, no Docker daemon needed in CI.",
      "etymology": "Open Container Initiative — the Linux Foundation working group that owns the spec.",
      "connector": "Like the W3C HTML spec for the web — the standard everyone implements, vendor-neutral.",
      "codeExample": {
        "language": "bash",
        "code": "# Inspect any OCI image (no Docker daemon needed)\nskopeo inspect docker://gcr.io/distroless/static\n\n# Build to OCI format directly\nbuildah bud -t myapp:1.0 .\nbuildah push myapp:1.0 docker://ghcr.io/me/myapp:1.0\n\n# All compliant runtimes can run it\ncontainerd, podman, docker, cri-o → all support OCI"
      },
      "unlockLevel": 2,
      "tags": [
        "docker",
        "standards"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0130-0000-0000-0000-000000000013",
      "word": "Ephemeral",
      "pronunciation": "ih-FEM-er-ul",
      "partOfSpeech": "adjective",
      "shortDefinition": "Short-lived, temporary nature of a container's local file system.",
      "simpleDefinition": "The design principle where container instances are treated as temporary and disposable — where local data is lost upon deletion. This encourages a stateless architectural approach, where persistent information is strictly managed through external volumes or databases rather than local storage.",
      "longDefinition": "In the Docker ecosystem, 'ephemeral' refers to the fact that any changes made to the container's writable layer are non-persistent. When a container is stopped and removed, that layer is deleted forever. This philosophy forces developers to build more resilient and scalable applications by separating the application logic from the data it processes. It ensures that any container can be replaced or moved to a different host without losing critical application state.",
      "connector": "Think of ephemeral like a whiteboard — you can write on it during a meeting, but it’s erased once the meeting is over.",
      "techContext": "Containers should be treated as ephemeral; you should be able to kill and replace any container at any time without losing critical state.",
      "exampleSentence": "Since the local container storage is ephemeral, we store all application logs in an external logging service.",
      "codeExample": {
        "language": "bash",
        "code": "# Run a container that is automatically removed after it exits\ndocker run --rm alpine echo \"I am temporary\""
      },
      "etymology": "from Greek ephēmeros ('lasting only a day').",
      "unlockLevel": 2,
      "tags": [
        "docker",
        "philosophy",
        "storage"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0140-0000-0000-0000-000000000014",
      "word": "Bind Mounts",
      "pronunciation": "BYND MOWNTS",
      "partOfSpeech": "noun",
      "shortDefinition": "Direct mapping of host machine directories into a container's filesystem.",
      "simpleDefinition": "High-performance storage method that maps a specific host path into a container — providing immediate synchronization between host and container files. They are indispensable for development, allowing developers to see code changes instantly without rebuilding the image.",
      "longDefinition": "Bind mounts have been available since the early days of Docker. When you use a bind mount, a file or directory on the host machine is mounted into a container. The file or directory is referenced by its absolute path on the host machine. Unlike volumes, bind mounts rely on the host machine having a specific directory structure. This makes them less portable than volumes but perfect for development-time source code sharing.",
      "connector": "Think of a bind mount like a shared folder between your laptop and a VM — changes on either side appear instantly.",
      "techContext": "I used a bind mount to map my local 'src' folder into the container so I could see my code changes immediately without rebuilding the image.",
      "exampleSentence": "Bind mounts are great for development, but for production data, Docker volumes are generally preferred for better performance and management.",
      "codeExample": {
        "language": "bash",
        "code": "# Mount current directory to /app inside the container\ndocker run -v $(pwd):/app -w /app node:20-alpine npm run dev"
      },
      "etymology": "from 'bind' (to fasten) + 'mount' (to set in position).",
      "unlockLevel": 3,
      "tags": [
        "docker",
        "storage",
        "development"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0150-0000-0000-0000-000000000015",
      "word": "Entrypoint",
      "pronunciation": "EN-tree-poynt",
      "partOfSpeech": "noun",
      "shortDefinition": "Fixed primary command executed when a container launches.",
      "simpleDefinition": "Immutable startup instruction defining the main executable for a container — ensuring consistent behavior by ignoring typical override arguments. It is often paired with CMD to provide default parameters while keeping the core application command constant.",
      "longDefinition": "The ENTRYPOINT instruction allows you to configure a container that will run as an executable. When you use the 'exec' form, Docker runs the specified command as PID 1, allowing it to receive Unix signals like SIGTERM properly. Unlike CMD, the entrypoint is not overridden when command-line arguments are passed; instead, those arguments are appended to the entrypoint, making it ideal for wrapper scripts or dedicated service containers.",
      "connector": "Think of the entrypoint like the ignition in a car — it’s the one action that always starts the engine running.",
      "techContext": "Often used for initialization scripts that set up a database or environment before starting the main application process.",
      "exampleSentence": "We used an entrypoint script to run database migrations before starting the web server.",
      "codeExample": {
        "language": "dockerfile",
        "code": "# Ensure the container always runs as an executable\nENTRYPOINT [\"/usr/bin/python3\", \"main.py\"]"
      },
      "etymology": "from 'entry' + 'point'.",
      "unlockLevel": 3,
      "tags": [
        "docker",
        "configuration",
        "lifecycle"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0160-0000-0000-0000-000000000016",
      "word": "CMD",
      "pronunciation": "SEE-em-dee",
      "partOfSpeech": "acronym",
      "shortDefinition": "Default execution command and parameters for a running container instance.",
      "simpleDefinition": "Dockerfile instruction providing fallback commands and arguments — which are easily overridden by user input at runtime. It defines the default entry point for the container's main process while remaining flexible enough for various operational needs and debugging.",
      "longDefinition": "The CMD instruction sets the command to be executed when a container starts, but it is less permanent than ENTRYPOINT. If a user provides arguments to the 'docker run' command, the CMD values are completely ignored in favor of the new input. In a Dockerfile, CMD is often used to provide default arguments to an ENTRYPOINT, creating a flexible interface where the main application stays fixed but its behavior can be tweaked easily.",
      "connector": "Think of CMD like the default settings on a new device — they work out of the box but are easy to change.",
      "techContext": "If you provide an ENTRYPOINT, CMD becomes the default arguments for that entrypoint.",
      "exampleSentence": "I used CMD to set the default log level for our app, which developers can override if they need more verbose output.",
      "codeExample": {
        "language": "dockerfile",
        "code": "# Set the default command, overrideable by 'docker run'\nCMD [\"npm\", \"run\", \"serve\"]"
      },
      "etymology": "Abbreviation for 'Command'.",
      "unlockLevel": 3,
      "tags": [
        "docker",
        "configuration"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0170-0000-0000-0000-000000000017",
      "word": "Docker Daemon",
      "pronunciation": "DAK-ur DEE-mun",
      "partOfSpeech": "noun",
      "shortDefinition": "Background process managing Docker objects and receiving API requests from clients.",
      "simpleDefinition": "Persistent background service — dockerd — that handles the heavy lifting of building, running, and distributing containers. It exposes a REST API that the Docker CLI communicates with to manage the lifecycle of various Docker objects.",
      "longDefinition": "The Docker daemon is the heart of the Docker architecture. It runs on the host machine and listens for requests from the Docker client. It is responsible for managing images, containers, networks, and volumes. The daemon can also communicate with other daemons to manage Docker services in a cluster. Because it requires root privileges to manage system resources, security configuration of the daemon is critical in production environments.",
      "connector": "Think of the Docker daemon like an engine room on a ship — you give orders from the bridge, and it works.",
      "techContext": "When you run 'docker build', your CLI client is actually sending a request to the Docker daemon running on your machine.",
      "exampleSentence": "I had to restart the Docker daemon because it was having trouble communicating with the container runtime.",
      "codeExample": {
        "language": "bash",
        "code": "# Check the status of the Docker daemon (on Linux)\nsystemctl status docker"
      },
      "etymology": "from Greek daimōn ('spirit, guardian'), used in computing for background processes.",
      "unlockLevel": 4,
      "tags": [
        "docker",
        "internals",
        "architecture"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0180-0000-0000-0000-000000000018",
      "word": "BuildKit",
      "pronunciation": "BILD-kit",
      "partOfSpeech": "noun",
      "shortDefinition": "Docker's modern build engine that adds parallel layer building, build-secret support, and cache mounts.",
      "simpleDefinition": "The newer Docker build backend (default since 23.0) — faster parallel builds, better caching, secret mounts that don't leak into layers.",
      "longDefinition": "BuildKit replaced the legacy Docker builder. Big wins: parallel stage execution (multi-stage builds run independent stages concurrently), cache-mount syntax for reusing things like apt/npm caches across builds, secret mounts that don't end up baked into image history. Frontend-agnostic too — you can build from Dockerfiles, Bazel rules, or custom DSLs.",
      "techContext": "Cache mounts (`RUN --mount=type=cache,target=/root/.npm npm install`) are the single biggest CI speedup. Secret mounts (`RUN --mount=type=secret,id=npmrc cat /run/secrets/npmrc`) replaced the dangerous practice of `COPY .npmrc` followed by `RUN rm .npmrc` — which left the secret in a layer.",
      "exampleSentence": "Switched the CI builds to BuildKit cache mounts — the npm install step went from 60s to 2s on cache hits.",
      "etymology": "Build (the core operation) + kit (a toolkit of capabilities).",
      "connector": "Like upgrading from a single-threaded compiler to a parallel one — same input, dramatically faster, more features.",
      "codeExample": {
        "language": "dockerfile",
        "code": "# syntax=docker/dockerfile:1.6\nFROM node:20-alpine\n\n# Cache mount: persist npm cache across builds\nRUN --mount=type=cache,target=/root/.npm \\\n    npm ci --omit=dev\n\n# Secret mount: don't bake into image history\nRUN --mount=type=secret,id=npmrc,target=/root/.npmrc \\\n    npm install @private/pkg"
      },
      "unlockLevel": 4,
      "tags": [
        "docker",
        "build",
        "tooling"
      ],
      "category": "tools"
    },
    {
      "id": "dock0190-0000-0000-0000-000000000019",
      "word": "Docker Swarm",
      "pronunciation": "DAK-ur SWORM",
      "partOfSpeech": "noun",
      "shortDefinition": "Native clustering and orchestration tool integrated into the Docker engine.",
      "simpleDefinition": "Integrated orchestration platform for managing multiple Docker hosts as a single virtual cluster — providing built-in service scaling and discovery. It simplifies distributed deployments by allowing developers to define service states that Docker automatically maintains across nodes.",
      "longDefinition": "Docker Swarm mode is built directly into the Docker Engine, allowing you to turn a group of Docker hosts into a cluster. It uses a declarative model to define the desired state of services, such as how many replicas of a container should be running. Swarm handles load balancing, health checks, and rolling updates, making it an excellent, lightweight alternative to Kubernetes for teams that prioritize simplicity and Docker-native integration.",
      "connector": "Think of Docker Swarm like a manager for a group of employees — it assigns tasks and makes sure the work continues.",
      "techContext": "While Kubernetes is more powerful, Docker Swarm is often preferred for smaller projects because it is much simpler to set up and manage.",
      "exampleSentence": "We used Docker Swarm to deploy our application across three servers for high availability.",
      "codeExample": {
        "language": "bash",
        "code": "# Initialize a swarm cluster and deploy a service\ndocker swarm init\ndocker service create --replicas 3 --name web-service nginx"
      },
      "etymology": "from 'Docker' + 'swarm' (a large group moving together).",
      "unlockLevel": 4,
      "tags": [
        "docker",
        "orchestration",
        "scaling"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0200-0000-0000-0000-000000000020",
      "word": "Distroless",
      "pronunciation": "DIS-troh-less",
      "partOfSpeech": "noun",
      "shortDefinition": "Minimal images containing only an application and its runtime dependencies.",
      "simpleDefinition": "Hardened container images lacking shells, package managers, or standard Unix utilities — containing only the application binary. They provide the highest level of container security by strictly limiting the tools available to an attacker.",
      "longDefinition": "Distroless images take the concept of minimalism further than Alpine. They contain only your application and its dependencies, stripped of everything else you would expect in a Linux distribution. Since there is no shell like bash or package manager like apt, common exploit techniques that rely on local tools are effectively neutralized. This makes distroless images an industry best practice for production environments where security is the highest priority.",
      "connector": "Think of a distroless image like a high-security vault — it only contains the treasure, with no tools left inside for thieves.",
      "techContext": "For our most critical security services, we use Google's distroless images to ensure that even if an attacker gets in, they don't even have a shell to run commands.",
      "exampleSentence": "We moved our production API to a distroless image to eliminate vulnerabilities related to unused OS utilities like curl or bash.",
      "codeExample": {
        "language": "dockerfile",
        "code": "# Build app in one stage, run in distroless\nFROM golang:1.21 AS build\nCOPY . .\nRUN go build -o app\n\nFROM gcr.io/distroless/static-debian12\nCOPY --from=build /app /app\nCMD [\"/app\"]"
      },
      "etymology": "from 'distribution' (Linux distro) + '-less'.",
      "unlockLevel": 5,
      "tags": [
        "docker",
        "security",
        "advanced"
      ],
      "category": "qualities"
    },
    {
      "id": "dock0210-0000-0000-0000-000000000021",
      "word": "Control Groups",
      "pronunciation": "kun-TROHL GROOPS",
      "partOfSpeech": "noun",
      "shortDefinition": "Linux kernel feature limiting and isolating resource usage for container processes.",
      "simpleDefinition": "Kernel mechanism — cgroups — used to monitor and restrict hardware resource consumption like CPU and memory. It prevents 'noisy neighbors' by ensuring that no single container can exhaust the host system's resources, maintaining overall stability.",
      "longDefinition": "Control groups, or cgroups, are a Linux kernel feature that allows Docker to partition resources such as CPU time, system memory, and network bandwidth. By assigning containers to specific cgroups, the Docker daemon can enforce limits defined at runtime. This isolation is critical for multi-tenant environments where you must guarantee that a runaway process in one container does not crash the entire host or starve other services of vital resources.",
      "connector": "Think of cgroups like a fuse box in a house — it prevents one faulty appliance from blowing the power for everyone.",
      "techContext": "When you run a container with '--memory=512m', Docker is actually using cgroups under the hood to enforce that limit.",
      "exampleSentence": "The container was killed by the OOM (Out Of Memory) killer because it exceeded the limit set by its control group.",
      "codeExample": {
        "language": "bash",
        "code": "# Run a container with limited CPU and memory resources\ndocker run -d --cpus=\"0.5\" --memory=\"256m\" nginx"
      },
      "etymology": "from 'control' + 'group'.",
      "unlockLevel": 5,
      "tags": [
        "docker",
        "linux",
        "internals"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0220-0000-0000-0000-000000000022",
      "word": "Namespaces",
      "pronunciation": "NAYM-spayss-uz",
      "partOfSpeech": "noun",
      "shortDefinition": "Linux kernel feature providing isolated views of system resources for containers.",
      "simpleDefinition": "Core isolation technology that provides containers with their own virtualized view of system resources — including process IDs and network stacks. It ensures processes in one container cannot see or interact with those in another or the host.",
      "longDefinition": "Namespaces are the primary building block of container isolation in Linux. They wrap a global system resource in an abstraction that makes it appear to the processes within the namespace that they have their own isolated instance of the resource. Different types of namespaces provide isolation for different aspects of the system, such as PIDs (process IDs), NET (networking), MNT (mount points), and UTS (hostname), effectively creating the illusion of a separate OS.",
      "connector": "Think of namespaces like private soundproof booths in a busy library — everyone is in one room, but can't hear each other.",
      "techContext": "Namespaces are the reason why 'process 1' inside a container is not the same as 'process 1' on the host machine.",
      "exampleSentence": "Docker uses network namespaces to give each container its own virtual network stack and IP address.",
      "codeExample": {
        "language": "bash",
        "code": "# See different PIDs inside and outside the container\ndocker run alpine ps aux"
      },
      "etymology": "from 'name' + 'space'.",
      "unlockLevel": 5,
      "tags": [
        "docker",
        "linux",
        "internals"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0230-0000-0000-0000-000000000023",
      "word": "Overlay Network",
      "pronunciation": "OH-vur-lay NET-wurk",
      "partOfSpeech": "noun",
      "shortDefinition": "Distributed virtual network enabling communication between containers on different host nodes.",
      "simpleDefinition": "Virtual network spanning multiple physical servers — allowing containers across a cluster to communicate securely as if on a local subnet. It abstracts the underlying physical network to provide seamless service discovery in distributed environments.",
      "longDefinition": "Overlay networks are used in the context of multi-host container clusters, such as Docker Swarm or Kubernetes. They use VXLAN technology to encapsulate traffic and route it between containers regardless of which physical host they reside on. This allows for high-level abstractions where services can be scaled across many nodes without the need to manage individual IP addresses or complex firewall rules for every cross-node communication path.",
      "connector": "Think of an overlay network like a private VPN for containers — it lets them talk privately across the entire internet.",
      "techContext": "Essential for Docker Swarm and Kubernetes, where containers are spread across a cluster of servers.",
      "exampleSentence": "The microservices communicate over an overlay network, so they don't need to know the physical IP addresses of the servers they are running on.",
      "etymology": "from 'overlay' (something laid over something else) + 'network'.",
      "unlockLevel": 5,
      "tags": [
        "docker",
        "networking",
        "orchestration"
      ],
      "category": "concepts"
    },
    {
      "id": "dock0240-0000-0000-0000-000000000024",
      "word": "containerd",
      "pronunciation": "kun-TAY-nur-dee",
      "partOfSpeech": "noun",
      "shortDefinition": "Lightweight, industry-standard container runtime for managing complete container lifecycles.",
      "simpleDefinition": "High-level container runtime that handles image transfer, storage, and execution across host systems — serving as a core component of Docker. It is designed to be embedded into larger systems like Kubernetes, focusing strictly on performance and reliability.",
      "longDefinition": "containerd is an industry-standard container runtime that was originally part of Docker but is now an independent project under the Cloud Native Computing Foundation. It handles all the low-level tasks required to run containers, such as managing namespaces and cgroups via runc. By decoupling the runtime from the higher-level Docker features, containerd provides a stable and secure foundation that can be used by various orchestration platforms to execute containerized workloads efficiently.",
      "connector": "Think of containerd like the engine of a car — while the car has seats and a radio, the engine does the work.",
      "techContext": "Docker itself now uses containerd under the hood to handle the actual running of containers.",
      "exampleSentence": "Since Kubernetes deprecated Docker-shim, we now use containerd directly as our container runtime on all our nodes.",
      "codeExample": {
        "language": "bash",
        "code": "# View containerd namespaces (advanced troubleshooting)\nctr namespaces list"
      },
      "etymology": "from 'container' + 'd' (for daemon).",
      "unlockLevel": 5,
      "tags": [
        "docker",
        "internals",
        "standards"
      ],
      "category": "components"
    },
    {
      "id": "dock0250-0000-0000-0000-000000000025",
      "word": "Overlay2",
      "pronunciation": "OH-vur-lay TOO",
      "partOfSpeech": "noun",
      "shortDefinition": "Modern, high-performance storage driver using union filesystems for image layers.",
      "simpleDefinition": "Default storage driver for modern Docker — utilizing a copy-on-write mechanism to manage layered filesystems efficiently. It enables rapid container startup and minimal disk overhead by stacking multiple read-only image layers into a single, unified view for the running process.",
      "longDefinition": "Overlay2 is the preferred storage driver for Linux distributions, providing superior performance and stability compared to older drivers like aufs or devicemapper. It works by creating a 'merged' view of several directories, where the 'lower' directories are the read-only image layers and the 'upper' directory is the container's writable layer. This allows Docker to share common files across many containers on the same host while maintaining the isolation required for each individual instance.",
      "connector": "Think of Overlay2 like a stack of transparent sheets — you can see everything below, but you only write on the top sheet.",
      "techContext": "We had to upgrade our kernel to ensure we could use the overlay2 driver for better disk I/O performance.",
      "exampleSentence": "Docker uses \"Overlay2\" as its default copy-on-write storage driver on most Linux systems.",
      "codeExample": {
        "language": "bash",
        "code": "# Check the storage driver currently in use by Docker\ndocker info | grep \"Storage Driver\""
      },
      "etymology": "from 'overlay' + version '2'.",
      "unlockLevel": 5,
      "tags": [
        "docker",
        "internals",
        "linux"
      ],
      "category": "concepts"
    }
  ]
}
